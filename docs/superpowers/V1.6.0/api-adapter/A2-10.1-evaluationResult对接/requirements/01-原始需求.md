# A2 Web 10.1 对接 evaluationResult · 原始需求

**日期：** 2026-09-09  
**档位：** 标准  
**来源：** 原 `scoring.summaryTable` 转换过重；改用 `aReport.evaluationResult`

## 要什么

1. Web A2「10.1 评分汇总表」改接 `aReport.evaluationResult`
2. **删除**原 10.1 基于 `scoring.summaryTable` 的组装逻辑（不影响其它章节 / 封面分 / G05）
3. 字段文档：`课堂评估-final.json`（`evaluationResult.dimensionList` + `totalCalc`）

## 用户裁定

1. `coreBasis` 后端会提供（可不在文档样例中）；缺失或空 → `--`
2. **小结设计**满分固定 **10**（不跟接口 `weight`）；其余维度满分 = `round(weight × 100)`（对齐图一百分比）

## 不做

- H5 本期
- 重做 10.1 视觉
- 展示 `scoreItems` 明细

## 实现仓

- `e:\code\frontend\src\pages\analysis-web\ai-teaching-diagnosis\`
- VO：`src/types/teaching-diagnosis-a2-report-vo.ts`
