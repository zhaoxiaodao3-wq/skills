# H5-A2 报告接口对齐 · 原始需求

**日期：** 2026-09-08  
**档位：** 标准  
**来源：** Web A2 已对接 `aReport` 并完成映射/条件渲染约定后，同步 H5 分享页

## 背景

- Web：`classroom-content-analysis-a2.mapper.ts` 已吃 `caseBasicInfo.aReport`
- H5：`E:\code\H5\src\pages\share\analysisTeachingA2\` 仍全 mock（`mapA2ToView` 恒回退）
- 旧模块 `feature/H5分享报告-A2` 已 DELIVERED（样式阶段）；本模块专做接口对齐

## 要什么

1. H5 A2 分享报告对接真实接口字段，**映射规则与 Web 保持一致**
2. **H5 禁止改样式**（不改 SCSS / 布局 / 视觉结构）；只做取数 + adapter 把接口内容填进**现有**组件槽位
3. **有对接冲突先列清单找人确认**，再改代码（见 `archive/spike-禁改样式-对接问题清单.md`）
4. **删掉无用逻辑**：FORCE_MOCK、Empty Probe、恒 mock 分支、无引用预览件；真数据后收缩/去掉大体量 mock

## 不做

- 改 H5 视觉样式 / Figma 还原 / 为对齐 Web 改栅格或新增展示组件样式
- G05 评分详情弹窗新字段（与 Web 一致：暂不管）
- 抽 Web mapper 为跨仓共享包（本期不推荐）
- 未经确认的「顺手改模板/组件结构」

## 权威约定（跟 Web）

- `docs/superpowers/V1.6.0/api-adapter/A2报告接口字段对齐/archive/spike-条件渲染字段约定-后端核对.md`
- `docs/superpowers/V1.6.0/api-adapter/A2报告接口字段对齐/archive/spike-3.6.2逻辑分析三段数组字段约定.md`
- 条件显隐**不读** `renderFlags`，只按字段启发式
- `preJudgment`：固定 4 项、**仅传值**；后端约定非空
- 3.6.2：`logicalClue` / `goalOrientation` / `closureAnalysis` 为 `string[]`；`questionChainAnalysis` null/`{}` 同构

## 主要改动仓

| 仓 | 路径 |
|----|------|
| H5 | `src/pages/share/analysisTeachingA2/**`（adapter / hook / types / mock 清理） |
| frontend（可选） | 分享 path：A2 应对 `analysis-teaching-a2`（若当前仍指 a1） |
