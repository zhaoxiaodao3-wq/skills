# H5 A2 10.1 对接 evaluationResult · 原始需求

**日期：** 2026-09-09  
**档位：** 标准  
**来源：** Web 已交付 `A2-10.1-evaluationResult对接`；H5 同理换源

## 要什么

1. H5 分享页 A2「10.1 评分汇总表」改接 `aReport.evaluationResult`（`dimensionList` + `totalCalc`）
2. **映射逻辑与 Web 一致**（满分、补偿、底栏、空态不回退）
3. **样式 / 组件结构保持 H5 现状**（`A2FieldCard` + `A2ScoreSummary`，不改 Vue/SCSS）
4. 删除/停用旧 `scoring.summaryTable` → 10.1 组装（`mapScoreSummaryTail`、`SUMMARY_TAIL_*` 等）

## 用户裁定（P1）

- 逻辑跟 Web；样式保持 H5 不变
- 时长系数 hint：H5 无独立 hint 槽，拼进 value（与现 `mapScoreSummaryTail` 展示习惯一致）

## 不做

- 改 H5 10.1 视觉 / 新增 hint UI
- 改 Web
- 其它章节重映射

## 实现仓

- `E:\code\H5\src\pages\share\analysisTeachingA2\`（主改 `adapters/mapA2ToView.ts`）
- 文档：本模块（frontend `docs/superpowers/...`）
