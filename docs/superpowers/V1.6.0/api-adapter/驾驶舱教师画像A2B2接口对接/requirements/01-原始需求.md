# 驾驶舱教师画像 A2/B2 接口对接 · 原始需求

**模块：** `api-adapter/驾驶舱教师画像A2B2接口对接`  
**实现仓：** `E:\code\dataView\apps-development-platform\apps\data-cockpit\`  
**页面：** `/data-cockpit-app/preview/teacher-portrait-detail`  
**接口文档：** 与校端相同（`a2DimensionScore` / `b2DimensionScore`）  
**对齐参考：** 已交付 `api-adapter/教师画像A2B2接口对接`（校端）  
**P1 确认：** 2026-09-04 — 同校端：真字段 + key=`knowledgeMastery` + 选项 B 空态轮播

## 用户原话（摘要）

驾驶舱 teacher-portrait-detail 同理有 A2/B2 雷达，按接口文档对接。

## 目标

驾驶舱详情「评价维度得分」雷达在真实 `getTeacherProfile` 下正确展示 A2/B2；缺/空 2 类字段时仍有标签与轮播，雷达为子类型视觉空态。

## 范围

1. VO：`a2DimensionScore` / `b2DimensionScore`（可空）
2. Adapter：优先新字段 → bySubtype → 空 A2/B2 占位；`FILL_MISSING_DIMENSION_SUBTYPE_2_MOCK=false`
3. `CATEGORY_2` / mock key → `knowledgeMastery`
4. 雷达面板：按**当前子类型** `isEmpty` 渲染空态（非整块面板）
5. 整块缺省 ViewModel 亦含空 A2/B2，保证可轮播

## 空态（选项 B，与校端一致）

- 有真分 → 正常五维  
- 无字段 / null / 全空 → **仍有 A2/B2 标签 + 轮播**，雷达视觉空态  
- 不注入 MOCK 假分  
- 不因缺 A2/B2 清空已有 A1/B1

## 非目标

- 不改环图、等级汇总、得分趋势交互（除非为子类型空态所必需的透传）
- 不改校端 `e:\code\frontend` 已交付代码
- 不删除 `dimensionScore` / `dimensionScoreBySubtype` 兼容

## 验收意向

- 真接口有 A2/B2 → 标签 + 真分  
- 无 2 类字段 → 可轮播到 A2/B2 且雷达空态  
- A1/B1 / levelStat / totalCount 行为不变  
