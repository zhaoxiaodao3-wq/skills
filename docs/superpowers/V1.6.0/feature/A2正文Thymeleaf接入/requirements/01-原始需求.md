# A2 正文 Thymeleaf 接入 · 原始需求

**日期：** 2026-09-11  
**模块：** `feature/A2正文Thymeleaf接入`  
**档位：** 全量  
**P1：** 方案 A（用户确认）

## 关联路径

| 角色 | 路径 |
|------|------|
| 目标模板 | `src/report/report/A2/ClassroomContentAnalysisReportA2.html` |
| A1 参考（已接入） | `E:\code\muban\analysis-service\src\main\resources\lessonTemplates\A\A1\ClassroomContentAnalysisReportA.html` |
| Web 字段 SSOT | `src/types/teaching-diagnosis-a2-report-vo.ts` |
| Web mapper / 条件 | `src/pages/analysis-web/ai-teaching-diagnosis/mappers/classroom-content-analysis-a2.mapper.ts` |
| 写法与坑 | `src/pages/analysis-web/ai-teaching-diagnosis/template-fix/TEMPLATE_CHECKLIST.md`、`src/report/FRONTEND_THYMELEAF_GUIDE.md` |
| 旧模块（已交付但 HTML 被 regen 冲掉） | `docs/superpowers/V1.6.0/feature/A2 PDF Thymeleaf 对接/` |

## 用户原话（摘要）

> A2 HTML 将提供给后端生成 PDF，需插入 Thymeleaf；字段路径参考 Web（已对接）；参考 A1 已做好的模板；参考项目内踩坑总结；不影响原样式逻辑；内容多，拆细分任务，生成全新任务执行。

## P1 确认

- **方案 A**：仅正文 HTML 全量接入 Thymeleaf + 更新指南映射表；按章拆 Task。
- **不做**：本地 `render-thymeleaf` / `gen:a2:pdf` / puppeteer 端到端（另开模块）。
- **不做**：封面、目录；不为 VO 多出字段加 DOM；不改 CSS/布局。

## 现状

- 当前 `ClassroomContentAnalysisReportA2.html`：**无** `xmlns:th` / `th:*`（约 2961 行纯静态）。
- 旧模块曾接入过，后被 `gen:ccar:a2` 覆盖，故本轮**新开模块**重做。

## 验收（需求级）

- [ ] 十章 + hero 已有 mock 位均接上 `th:*`，路径对齐 `PostClassReportA2VO` / Web
- [ ] 条件渲染对齐 Web `mapFlags` / 章节显隐约定
- [ ] 空安全与 A1 一致；标签内默认 mock 保留
- [ ] 样式/DOM 骨架不变；`FRONTEND_THYMELEAF_GUIDE.md` 补 A2 映射
- [ ] 按章任务可逐项勾选交付
