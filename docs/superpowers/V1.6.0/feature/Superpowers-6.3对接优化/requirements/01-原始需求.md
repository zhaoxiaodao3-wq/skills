# Superpowers 6.3 对接优化 · 原始需求

**日期：** 2026-08-25  
**分类：** feature  
**提出方式：** `/harness` + 用户确认方案顺序

## 需求原文

检查官方 Superpowers 是否有版本更新；有则列出更新内容，并评估本仓库是否需要更新。  
用户确认理解：本仓库 = 官方 skill + 自建流程；自建流程调用官方 skill。  
**选定顺序（选项 1）：** 先把官方 Superpowers 更新到最新（v6.3.0），再结合当前 Harness 流程产出基于最新版的优化方案并落地。

## 目标

1. 本机官方插件升到 **v6.3.0**（Cursor / Claude Code，由用户操作）
2. 产出对接优化方案：官方能力如何接入自建 Harness，冲突时本地规则优先
3. 按确认后的方案改自建层（skills / 规则文档），不整包覆盖官方 skill

## 约束

- 禁止写入官方默认扁平路径 `docs/superpowers/specs/`（无版本 / type / 模块层）
- 自建门禁保留：P1 方案确认 → P2 spec 确认 → P3 执行方式；改 `src/` 前 `READY_TO_DEV`
- 用户未要求时不自动 commit

## 成功标准（待写入 spec）

- 官方插件已更新且会话 bootstrap 正常（用户侧验收）
- 对接关系文档化（官方 vs 自建职责边界清晰）
- harness-run / 相关规则吸收 6.3 有价值点（如三路分流与 fix 轻量对齐），且不削弱目录规范与交付自检
