# 教师画像并入分享 Registry · 原始需求

**工程：** `E:\code\H5\`  
**档位：** 标准  
**P1 确认：** 2026-08-31 — 轻并入：Registry + 路由 + OG/Vite 同源；**不改** `/teacher-profile` 路径与线上分享文案/封面

## 目标

将 `src/pages/share/teacherProfile` 挂入现有分享 Registry 流程，与课后报告共用「登记 → 路由 → Family HTML 映射」扩展方式。

## 范围（轻并）

1. `registry.ts` 登记 `teacher-profile`（独立 Family，非 `analysis-teaching-*`）
2. 路由由 registry 生成，去掉 `router/index.ts` 手写条目
3. Vite middleware / `FAMILY_OG_HTML` 与 registry 同源（去掉硬编码 teacher-profile 特例或改为读 registry）
4. 更新概要/架构文档：两套链接格式并存

## 非目标

- 不改分享 URL、title/desc/image 取值
- 不强制改为 `useShareReportSession`（保留 `useTeacherProfileShare`：`token` 兼容、分级失效）
- 不改画像页业务模块与 `ShareInvalidState` 行为
- 不新建 HTML / 不改 Nginx location 文案行为（仅注释可对齐文档）

## 验收意向

- registry 含 teacher-profile；打开 `/teacher-profile?code=` 行为与改前一致
- 新增课后报告类型的扩展方式不变；画像作为独立 Family 写在概要文档中
