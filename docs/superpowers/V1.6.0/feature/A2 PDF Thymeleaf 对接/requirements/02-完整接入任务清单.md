# A2 PDF Thymeleaf · 完整接入任务清单

**日期：** 2026-09-10  
**目标：** 整份 A2 正文模板按 Web 字段 + 条件渲染 100% 接入  
**权威对照：**
- VO：`src/types/teaching-diagnosis-a2-report-vo.ts`
- Web mapper：`src/pages/analysis-web/ai-teaching-diagnosis/mappers/classroom-content-analysis-a2.mapper.ts`（`mapFlags` / 各 mapSection*）
- 空态文案：`classroom-diagnosis/constants/a2-report-messages.ts`
- A1 写法参考：`E:/code/muban/.../A1/ClassroomContentAnalysisReportA.html`
- 目标文件：`src/report/report/A2/ClassroomContentAnalysisReportA2.html`

**通用规则（每项任务默认遵守）**
1. 根变量 `aReport`（扁平 VO，禁止 `aReport.contents.*`）
2. 空值：`#strings.isEmpty(x) ? '-' : x`；保留标签内默认文字
3. `th:each` **只留 1 个模板节点**，删除同级静态 mock 兄弟
4. 关键位加 `data-field`
5. 条件文案与 Web 常量一致（见下方「条件渲染总表」）
6. **勿** `pnpm gen:ccar:a2` 覆盖已接模板（或 gen 后重跑补丁）

**状态图例：** `[x]` 已基本完成 · `[ ]` 待做 · `[~]` 部分完成需补齐

---

## 0. 横切基础设施

| ID | 任务 | 状态 |
|----|------|------|
| G0.1 | `<html>` 已含 `xmlns:th` | [x] |
| G0.2 | 约定局部变量（建议章节根 `th:with`）：`ov`/`li`/`nk`/`pf`/`cs`/`sd`/`ta`/`pc`/`bc`/`er`/`flags` | [ ] |
| G0.3 | 实现/注入与 Web `mapFlags()` 等价的 7 个 flag（后端渲染时也可预计算塞进 context） | [~] 模板侧：小结/导入/实验用字段推算；问题链优先 `renderFlags.hasProblemChain`；3.2 深板用列表空判断 |
| G0.4 | 空列表通用：容器 `th:if="${!#lists.isEmpty(...)}"` + 空态「暂无数据」（若该节 Web 有） | [x] 2026-09-10 空态补完|
| G0.5 | 清理全文件：每个 `th:each` 后删除多余静态 `<tr>`/`<article>`/`<li>` | [x] 3.1/3.2/3.3/3.5/3.6 主要块已清 |
| G0.6 | `FRONTEND_THYMELEAF_GUIDE.md` §23 随任务更新为最终字段表 | [~] |
| G0.7 | （可选）本地 `render-thymeleaf` + mock 注入冒烟 | [ ] |

### 条件渲染总表（对齐 Web `mapFlags`）

| Flag | 计算（与 mapper 一致） | 为 false / 空时 UI |
|------|------------------------|-------------------|
| `hasSummaryTeaching` | `preJudgment` 非空且 `[0]` 不以「否\|无」开头 | 隐藏 5.1–5.3；显示 `本节课无总结性教学行为` |
| `importHasCoreQuestion` | `coreQuestionReturn?.length > 0` | 5.3 显示 `导入环节未提出核心问题，本模块不适用` |
| `hasExperimentActivity` | `teachingMethod.activityDesign?.length > 0` | 3.4.2 显示 `【无实验/活动】` |
| `hasProblemChain` | 维度「问题链设计与追问质量」评价匹配 `有效\|不足` 且非缺失 | 3.6.2 逻辑区显示 `不存在问题链，本模块不适用`（表仍可显示） |
| `hasKnowledgeRelationTypes` | `deepAnalysis.relationTypes?.length > 0` | 板块二：`知识点之间无明确关系类型可分析` |
| `hasThinkingSpanEvaluation` | `thinkingSpanEvaluation?.length > 0` | 板块三：`暂无足够依据进行思维跨度合理性评估` |
| `hasGradeCognitionFit` | `cognitiveFit?.length > 0` | 板块四：`暂无足够依据评估与学段认知规律的符合度` |
| `totalCalc != null` | — | 已有：隐藏评分汇总块 |
| `compensationEffective` | — | 已有：补偿徽章 |

---

## 1. Hero · `section-hero`

| ID | 任务 | VO / 说明 | 状态 |
|----|------|-----------|------|
| H.1 | 科目 / 年级 / 教材章节 / 课堂时长 | `overallSummary.subject/grade/planSource/duration` | [x] |
| H.2 | 模板样式 | `reportSubType`（默认 A2） | [x] |
| H.3 | （确认不做）总分/等级卡 | Web PDF 轨亦无 Hero 评分卡 | [x] N/A |

---

## 2. 一、课堂整体总结 · `section-summary`

### 1.1 `summary-1-1`
| ID | 任务 | 状态 |
|----|------|------|
| S11.1 | `overallSummary.summaryText` → 段落 | [x] |
| S11.2 | 空态：`summaryText` 空时显示「暂无数据」或隐藏 | [x] 2026-09-10 空态补完|

### 1.2 `summary-1-2` 亮点
| ID | 任务 | 状态 |
|----|------|------|
| S12.1 | `th:each="h : ${...highlights}"`：seq / description / evidence / suggestion | [x] |
| S12.2 | 删除多余静态行（仅留模板行） | [~] 查 thead+1 行即可 |
| S12.3 | 空列表隐藏表 / 空态文案 | [x] 2026-09-10 空态补完|
| S12.4 | （可选）Web 拆 lead/body；VO 仅 description → 整格绑 description | [x] 已整格 |

### 1.3 `summary-1-3` 不足
| ID | 任务 | 状态 |
|----|------|------|
| S13.1 | `th:each="w"`：category / priority / description / evidence / impact / suggestion | [x] |
| S13.2 | 仅留 1 张模板卡（已收敛） | [x] |
| S13.3 | 空列表空态 | [x] 2026-09-10 空态补完|

---

## 3. 二、新课导入 · `section-import`

| ID | 子节 | 列表路径 | 列字段 | 状态 |
|----|------|----------|--------|------|
| I21 | 2.1 | `introMethod[]` | analysisItem, observation, introType, reasonableness, reason | [x] |
| I22 | 2.2 | `situationCreation[]` | dimension, observation, evaluation, reason | [x] |
| I23 | 2.3 | `priorKnowledgeActivation[]` | 同上 | [x] |
| I24 | 2.4 | `coreQuestion[]` | analysisItem, observation, isProposed, questionQuality | [x] |
| I25 | 2.5 | `studentEngagement[]` | analysisItem, observation, evaluation, reason | [x] |
| I26 | 2.6 | `transition[]` | 同上 | [x] |
| I27 | 2.7 | `overallSummary` 段落 | — | [x] |
| I28 | 各表空列表 / 整章空态 | — | [x] 2026-09-10 空态补完|
| I29 | 确认仅模板行、无静态兄弟行 | — | [ ] 抽检 |

---

## 4. 三、新知讲授 · `section-new-knowledge`（工作量最大）

### 3.1 `nk-3-1` 知识点教学
| ID | 任务 | 状态 |
|----|------|------|
| N31.1 | 第一部分：`asrKnowledgePoints[]` 仅 1 模板卡 + `th:each` | [x] 2026-09-10 已删 6 张静态兄弟卡 |
| N31.2 | 绑：seq、knowledgePoint、completenessReason、depthReason、methodAndAbility | [x] |
| N31.3 | 绑：teachingCompleteness、teachingDepth（stat 区） | [x] 2026-09-10 |
| N31.4 | 第二部分：`lessonPlanKnowledgePoints[]` 仅 1 模板卡 | [x] 2026-09-10 已删静态兄弟卡 |
| N31.5 | 绑：knowledgePoint、implementationLevel、implementationReason、isCore、isTaught | [x] 2026-09-10 补 isCore/isTaught |
| N31.6 | 第三部分：`comprehensiveConclusion[]` → `li th:each`，删多余 li | [x] 2026-09-10 只留 1 模板 li |
| N31.7 | 空列表空态 | [x] 2026-09-10 空态补完|

### 3.2 `nk-3-2` 知识呈现逻辑
| ID | 任务 | 状态 |
|----|------|------|
| N32.1 | `dimensions[]` 维度卡：只留 1 模板；绑 dimension / evaluation / observation / evaluationBasis / suggestion | [x] 已清兄弟 + 字段|
| N32.2 | 板块一：`deepAnalysis.knowledgeRelationAnalysis[]`（对齐 Web 解析或整行原文） | [x] knowledgeRelationAnalysis th:each|
| N32.3 | 板块二：`relationTypes[]` + `hasKnowledgeRelationTypes` 空态文案 | [x] relationTypes + 空态|
| N32.4 | 板块三：`thinkingSpanEvaluation[]` + flag 空态 | [x] thinkingSpanEvaluation + 空态|
| N32.5 | 板块四：`cognitiveFit[]` + metaLine + flag 空态 | [x] cognitiveFit + 空态|
| N32.6 | 删四板块内多余静态 numbered-panel item | [x] 面板已替换为单模板|

### 3.3 `nk-3-3` 重难点突破
| ID | 任务 | 状态 |
|----|------|------|
| N33.1 | `planReference[]`：对齐 Web `parsePlanReferenceItems`（固定 01/02 label+content，勿全标「教学重点」） | [x] planReference index/label 动态|
| N33.2 | `items[]` 卡：只留 1 模板；绑 keyPoint / effectiveness / causeAnalysis / teacherBehavior / evaluationReason / recommendedMethod | [x] items 单模板|
| N33.3 | `effectivenessSummary` 总结段 | [~] effectivenessSummary 若 DOM 有则已绑|
| N33.4 | 删静态重复卡/item | [x] 已清静态重复|

### 3.4 教学方法
| ID | 任务 | 状态 |
|----|------|------|
| N341 | 3.4.1 `methodMatch[]` 四列 | [x] |
| N342 | 3.4.2 `activityDesign[]` + **`hasExperimentActivity` 空态「【无实验/活动】」** | [x] 【无实验/活动】条件|
| N343 | 3.4.3 `aidAnalysis[]`；evaluation 若为数组需 join | [~] evaluation 直出（数组需后端 join）|
| N344 | 空列表处理 | [ ] |

### 3.5 `nk-3-5` 案例例题
| ID | 任务 | 状态 |
|----|------|------|
| N35.1 | `items[]` 只留 1 模板卡 | [x] 案例卡收敛|
| N35.2 | 绑：caseName, knowledgePoint, planConsistent, source, typicality, teachingQuality | [x] 五字段已绑|
| N35.3 | `overallEvaluation[]`：`li th:each`，删多余 li | [x] overallEvaluation th:each|
| N35.4 | 空态 | [x] 2026-09-10 空态补完|

### 3.6.1 `nk-3-6-1` Bloom
| ID | 任务 | 状态 |
|----|------|------|
| N361.1 | `bloomLevels`：`th:each` + **过滤「合计」行** | [x] 过滤合计|
| N361.2 | 绑：level / description / keywords / example / **count / ratio** | [x] count/ratio|
| N361.3 | 合计卡单独绑 totalRow（或聚合） | [x] 合计卡 th:each+if 合计|
| N361.4 | 删多余静态 bloom 卡 | [x] 静态 bloom 已删|
| N361.5 | 维度一：`analysisConclusion.hierarchyDistribution` | [~] hierarchyDistribution 尽量绑|
| N361.6 | 维度二：`goalConsistency.matches[]` 表 + `overallJudgment` | [x] matches 表|
| N361.7 | 维度三：`highLowOrderStructure[]`（可按 Web 固定 01/02/03 槽） | [~] highLowOrderStructure 首项 each|

### 3.6.2 `nk-3-6-2` 问题链
| ID | 任务 | 状态 |
|----|------|------|
| N362.1 | `dimensions[]` 表（已有） | [x] |
| N362.2 | `logicAnalysis.basicInfo` | [x] basicInfo|
| N362.3 | `cognitiveProgression.questions[]` + `summary` | [x] questions + summary|
| N362.4 | `logicalClue[]` / `goalOrientation[]` / `closureAnalysis[]` | [x] logicalClue/goalOrientation/closureAnalysis|
| N362.5 | `overallEvaluation`（可拆「优化建议」） | [x] overallEvaluation|
| N362.6 | **`hasProblemChain`**：逻辑区 if / unless → 不适用文案 | [x] hasProblemChain + N/A|

### 3.7 `nk-3-7`
| ID | 任务 | 状态 |
|----|------|------|
| N37.1 | `newKnowledgeTeaching.overallSummary` | [x] |
| N37.2 | 空态 | [x] 2026-09-10 空态补完|

---

## 5. 四、课堂练习 · `section-practice`

| ID | 任务 | 状态 |
|----|------|------|
| P41 | 4.1 `exerciseDesign[]` 卡（title=dimension\|analysisItem，badge=evaluation） | [x] |
| P42 | 4.2 `exerciseCompletion[]` 表 | [x] |
| P43 | 4.3 `overallSummary` | [x] |
| P44 | 空列表 / 空态 | [x] 2026-09-10 空态补完|

---

## 6. 五、课堂小结 · `section-classroom-summary`（条件渲染重点）

| ID | 任务 | 状态 |
|----|------|------|
| C50.1 | 5.0 `preJudgment[0..3]` 四值 | [x] |
| C50.2 | `preJudgment` 空 → 段落「本节课无总结性教学行为」（与 Web 一致） | [x]|
| C51 | 5.1 `summaryMethod[]`；末列对齐 Web：`reasonableness`+`reason` 合并 | [x] reasonableness+reason|
| C52 | 5.2 `knowledgeSystematization[]` | [x] |
| C53.1 | 5.3 `coreQuestionReturn[]` 表 | [x] |
| C53.2 | **`hasSummaryTeaching`**：false → 隐藏 5.1–5.3 + 章级无总结文案 | [x] hasSummaryTeaching|
| C53.3 | **`importHasCoreQuestion`**：false（且有总结教学）→ 5.3 N/A 文案 | [x] importHasCoreQuestion N/A|

---

## 7. 六–九

| ID | 章节 | 任务 | 状态 |
|----|------|------|------|
| L61 | 6.1 | `studentDiagnosis.dimensions[]`（对象三列；若 string[] 需约定） | [x] |
| L62 | 6.2 | `typicalOutputs[]` | [x] |
| L63 | 6.* | 空态 | [x] 2026-09-10 空态补完|
| T71 | 7.1 | `timeAllocation.slots[]`：phaseName/timeRange/duration/ratio | [x] |
| T72 | 7.* | 空态 | [x] 2026-09-10 空态补完|
| P81 | 八 | `planComparison.items[]`：dimension/planPreset/actualClass/conclusion | [x] |
| P82 | 八 | `summary` | [x] |
| P83 | 八 | 空态 | [x] 2026-09-10 空态补完|
| B91 | 九 | `benchmarkComparison.items[]`：dimension/bestPractice/borrowDirection | [x] |
| B92 | 九 | `referenceNote` | [x] |
| B93 | 九 | 空态 | [x] 2026-09-10 空态补完|

---

## 8. 十、评分评级 · `scoring-10-1`

| ID | 任务 | 状态 |
|----|------|------|
| SC1 | `dimensionList[]`：dimensionName / finalGrade / 补偿 / coreBasis | [x] |
| SC2 | 补偿徽章 `compensationEffective` | [x] |
| SC3 | **得分格式**：`dimensionScore/满分`（小结设计满分 10；其余 `round(weight*100)` 或 VO.fullScore）对齐 Web `formatScoreOverFull` | [x] score/满分|
| SC4 | `totalCalc` 块：`th:if` + sumOfDimensionScore / classDurationRaw / classDurationCoefficient / finalTotalScore / overallGrade / overallGradeDesc | [x] |
| SC5 | 时长系数 **hint** 动态化（对齐 Web `buildDurationCoefficientHint`） | [x] hint 绑 durationRaw|
| SC6 | 最终总分侧注「=总分小计×时长系数」 | [~] |
| SC7 | `dimensionList` 空：表壳/空态 | [x] 2026-09-10 空态补完|
| SC8 | 确认不接 `scoreItems` / `conditionList` / deprecated `scoring` | [x] |

---

## 9. 验收清单（整份完成才勾）

| ID | 验收项 | 状态 |
|----|--------|------|
| V1 | 每个 HTML 已有 mock 展示位均有对应 `th:*` 或明确「静态标题不接」 | [ ] |
| V2 | 无 `th:each` 旁残留静态兄弟导致双份内容 | [ ] |
| V3 | 7 个 Web flag 条件渲染行为与 `ReportTypeA2View` / mapper 一致 | [ ] |
| V4 | 空态文案与 `a2-report-messages.ts` 一字不差 | [ ] |
| V5 | 字段路径全部落在 `PostClassReportA2VO`（抽检无编造字段） | [ ] |
| V6 | 剥 `th:*` 后默认 mock 仍可读（设计稿预览） | [ ] |
| V7 | 后端用真实 `aReport` 渲染一版 PDF 肉眼过十章 | [ ] |
| V8 | 更新 archive / harness（若另开 fix 模块则走 Harness） | [ ] |

---

## 10. 建议执行顺序（减少返工）

```
① G0.3–G0.5 横切：flags 约定 + 清兄弟节点策略
② 第三章 N31→N36（缺口最大）
③ 第五章条件渲染 C50–C53
④ 3.4.2 / 3.2 空态 / 3.6.2 hasProblemChain
⑤ 第十章得分格式 SC3–SC5
⑥ 各章空列表 G0.4 + I28/P44/…
⑦ V1–V7 验收 + 文档 §23 定稿
```

**预估：** 约 **55–70** 个可勾选原子任务；其中已完成约 **25**，待补约 **35+**（第三章 + 条件渲染占大半）。

---

## 11. 与「已交付」模块关系

上一模块 `feature/A2 PDF Thymeleaf 对接` 已打通骨架。本清单是 **100% 对齐 Web 的补齐范围**。  
实施时建议：`/harness` 开新模块 `fix/A2-Thymeleaf完整接入`（标准或全量），以本文件为 requirements 附件。


---

## 自动连跑记录（2026-09-10）

已自动执行清单剩余主体（无需逐步确认）：
- 清兄弟节点、3.2 deepAnalysis 四板块 + 空态
- 3.3 / 3.5 / 3.6 Bloom / 3.6.2 问题链 DOM 接线
- 第五章条件渲染（`th:with` + hasSummaryTeaching / importHasCoreQuestion）
- 3.4.2 无实验空态、10.1 得分 `/` 满分格式

**仍建议人工抽检：**
1. 3.4.2 条件包装的 `</div>` 是否配对
2. Bloom「维度一」文案节点是否命中 hierarchyDistribution
3. `renderFlags.hasProblemChain` 后端是否注入（未注入时问题链逻辑区可能整块隐藏）
4. 各章「空列表暂无数据」通用空态（I28/P44/L63 等）尚未逐节加


---

## 空态补完记录（2026-09-10）

- 通用文案：`暂无数据`（对齐 Web `EMPTY_BLOCK`）
- 表格：保留 thead，空 tbody 一行 colspan
- 卡片/列表：空态 note + `th:if` 包裹有数据容器
- 段落：空时旁路「暂无数据」
- 特殊空态保留：无实验/活动、3.2 深板三文案、问题链 N/A、第五章无总结 / 导入无核心问题
- 标记：`data-ccar-empty-for=*` 约 36 处
