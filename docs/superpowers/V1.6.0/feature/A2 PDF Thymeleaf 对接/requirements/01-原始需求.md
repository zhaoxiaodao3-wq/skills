# A2 PDF Thymeleaf 对接 · 原始需求

**日期：** 2026-09-10
**模块：** `feature/A2 PDF Thymeleaf 对接`
**关联文件：**
- `src/report/report/A2/ClassroomContentAnalysisReportA2.html`（PDF 模板，待改造）
- `src/report/FRONTEND_THYMELEAF_GUIDE.md`（A/B 类对接规范，参考）
- `src/pages/analysis-web/ai-teaching-diagnosis/template-fix/TEMPLATE_CHECKLIST.md`（A1 接入模板语法规范，**必读避免踩坑**）
- `src/pages/analysis-web/ai-teaching-diagnosis/classroom-diagnosis/mappers/classroom-content-analysis-a2.mapper.ts`（web 端 mapper，含 `A2EvaluationResultVO` 字段）
- `src/types/teaching-diagnosis-a2-report-vo.ts`（接口 VO 定义）

---

## 用户原话

> 看起来基础样式没啥问题了，接下来就该对接模板引擎的模板语法了，接口的话跟 web 的完全一致，你分析下有不懂的问题，分析好列好任务执行
>
> 只接已有的，条件渲染尤其要注意，并且之前接入 A1 的时候总结了一些关于模板语法的坑，你找找有没有这个文档，可以参考下，避免坑

**关键决策（用户回答 14 个问题后确定）：**

1. 根变量 = `aReport`（和 A1 写法一致）
2. 后端接口已存在（`/analysis/selfAnalysisResult` 的 `caseBasicInfo.aReport`，走 web 的 `PostClassReportA2VO`）
3. 字段路径 = `${aReport.章节字段.子字段}`（章节字段直接挂在 aReport 下）
4. **只接 HTML 已有 mock 位**（§0 FRONTEND_GUIDE 核心原则），VO 多出字段**不接**、**不加 DOM**
5. 空态处理：后端没注入就显示**空态 UI**（用 `th:if` 判空兜底）
6. **拆分成章节任务一个一个实现**（按章节 PR）
7. 第十章"本课堂评分评级"接 `aReport.evaluationResult.dimensionList + totalCalc`（即 web 端 `mapEvaluationResultToScoring101` 的输入字段）
8. **前端搭一个完整生成 PDF 的流程**（mock → Thymeleaf 渲染 → HTML → puppeteer → PDF → 合并封面/目录/正文），不依赖后端就能本地端到端验证
9. 底部"温馨提示：评分与等级仅供参考..."**静态文案**（不接字段）
10. 第十章 5 维度表无顺序约束（用 `th:each` 循环）
11. 接口字段路径以 web 端 mock `evaluationResult` 字段为基准（与后端 `A2EvaluationResultVO` 一致）
12. 字段命名跟 web 一致
13. 体积变化无所谓
14. 写法按最标准（A1 经验 + TEMPLATE_CHECKLIST）

---

## 现状探查（已做完）

- ✅ `ClassroomContentAnalysisReportA2.html` 是已生成的 PDF 静态 HTML（183KB → 144KB，最近刚 gen 过），含完整章节结构
- ✅ `gen-ccar-a2-static-html.mts` 读 web mock 直接拼 HTML（绕开 Thymeleaf）
- ✅ `thymeleaf-local-preview.mjs` 剥 `th:*` 保留默认文字（不真正执行 Thymeleaf 表达式）
- ✅ `mapEvaluationResultToScoring101(er: A2EvaluationResultVO)` 已存在（web 端把 evaluationResult 转成 5 维度表 + 5 汇总行）
- ✅ A1 模板语法规范 `TEMPLATE_CHECKLIST.md` 13 节齐全
- ✅ `PostClassReportA2VO` 接口字段（顶层 11 个字段，每个对应 A2 报告一个章节）

## 范围

### 必做

1. **A2 PDF 模板改造**：10 章节（hero / summary / 导入 / 新知 / 练习 / 小结 / 学情 / 时间分配 / 教案对比 / 优秀课例 / 评分评级）按 mock 已有位接 Thymeleaf，每章节独立 PR
2. **Thymeleaf 渲染器**：`scripts/render-thymeleaf.mjs`（最小实现 `th:text` / `th:each` / `th:if` / `th:with` / 安全导航 / 判空 / `?.`）
3. **本地 PDF 流程**：`scripts/gen-a2-pdf.mjs`（mock → 渲染 → HTML → puppeteer → PDF → 合并封面/目录/正文）+ `pnpm gen:a2:pdf` 脚本
4. **§9 A2 字段映射**追加到 `FRONTEND_THYMELEAF_GUIDE.md`（与 §7/§8 平行）

### 不做

- 不接 VO 多出但 HTML 未展示的字段
- 不新增任何 DOM / section / 块
- 不搬 Vue computed 逻辑到模板
- 不接 `evaluationResult.scoreItems[]`（弹窗用，PDF 不嵌弹窗）
- 不接 `evaluationResult.dimensionList[].compensationCheck.conditionList[]`（PDF 只展示 5 维度汇总表，不展开）
- 不动封面/目录 PDF HTML（spec §2 明确）

## 验收

- [ ] 10 章节每章节独立 PR，每章节字段映射表 100% 覆盖 HTML 已有 mock 位
- [ ] 所有表达式遵守 A1 经验 15 条约束（详见 spec）
- [ ] `pnpm gen:a2:pdf` 一键跑通，输出最终 PDF
- [ ] PDF 视觉与 gen-ccar-a2 输出对齐（章节、文案、条件渲染都一致）
- [ ] 标签内默认文字保留（`pnpm preview:thymeleaf` 验证剥 th: 后能显示）
- [ ] 关键字段加 `data-field` 标注
- [ ] `pnpm check:ccar:a2` PASSED（R11 静态 + vitest 单元测试）
- [ ] `pnpm harness:check` 无本模块相关警告
- [ ] `pnpm harness:status` 阶段 = `DELIVERED`

## 本次补充（2026-09-10 下午）

用户再次确认：A2 PDF 模板需接入 **Thymeleaf** 占位语法，写法对齐已上线的 A1 模板。

| 项 | 路径 |
|----|------|
| A2 待改模板 | `src/report/report/A2/ClassroomContentAnalysisReportA2.html`（当前 **无** `th:*` / `xmlns:th`） |
| A1 权威参考（muban） | `E:/code/muban/analysis-service/src/main/resources/lessonTemplates/A/A1/ClassroomContentAnalysisReportA.html` |
| 前端规范 | `src/report/FRONTEND_THYMELEAF_GUIDE.md` |

**语法对齐 A1（踩坑规避）：**

1. `<html>` 加 `xmlns:th="http://www.thymeleaf.org"`
2. 空值优先 `#strings.isEmpty(x) ? '-' : x`，少用 Elvis `?:`（空字符串踩坑）
3. 标签内 **保留默认 mock 文字**
4. 只接 HTML 已有展示位；不新增 DOM；不接 VO 多出字段
5. 根变量 `aReport`，字段路径走 A2 扁平 VO（`overallSummary` 等），**不要**套 A1 的 `aReport.contents.*`
6. 条件渲染用 `th:if` / `th:unless`；列表用 `th:each`
7. 关键位加 `data-field`

**与已有 plan 关系：** 沿用 `plans/02-muban-based-batch-patch.md`（按 A1 写法接入），字段映射以 A2 VO 为准。

