# H5 分享报告 A2 · 原始需求

**模块：** `feature/H5分享报告-A2`  
**实现仓：** `E:\code\H5\`  
**Web 参考：** `e:\code\frontend` 课堂教学内容分析 A2（mock / types / flags / blocks）

## 用户原话（摘要）

针对已完成的 Web 版 A2 报告，在 H5 `src/pages/share` 新增 A2 分享页。Figma 设计稿拆为三帧（长页切片）。当前为**样式阶段**，无真实接口 → 全量 mock，并预留替换真实接口。按 H5「新增子类型」流程落地；mock/转换对齐 Web。组件化工程化、移动端多机型、高度还原；图标优先项目已有 → SVG → Figma 导出。

**P1 补充（已确认）：** Figma MCP 还原须**细粒度拆任务**；**每还原完一块先做样式审查，通过后再做下一块**。

## Figma

| 顺序 | 节点 | 名称 | 说明 |
|------|------|------|------|
| 1 | `8785:57152` | 报告A-1 | Cover+目录+章一～三入口 |
| 2 | `8785:57590` | 报告A-2 | 章三主体 |
| 3 | `8785:58244` | 报告A-3 | 章四～十+温馨提示 |

fileKey：`vmbLwcwclGPoT3fWJWv7de`

## 约束

- 路径：`/analysis-teaching-a2`；Family `a`；新 Template `analysisTeachingA2`
- 分享文案默认与 a1 同源
- 不改 a1/b1/b2/teacher-profile 现网行为
- 三帧正文课例不一致 → **数据以 Web A2 mock 为准**，Figma 管样式

## 补充（空值兜底 · 开发中修订）

接口字段可能缺失/空字符串。所有**标量展示字段**（Cover kv、总览 kv、评分数字/等级、正文块内字段值等）统一空态显示 **`--`**。  
例：总览「报告模板」无值时右侧为 `--`，不得留白。  
整块「章节不存在 / flags 不适用」仍走 Web A2 flags 语义（不适用态），与单字段 `--` 区分。
