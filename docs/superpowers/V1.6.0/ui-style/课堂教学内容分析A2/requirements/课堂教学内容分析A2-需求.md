# 课堂教学内容分析 A2 · 需求归档

**模块：** `ui-style/课堂教学内容分析A2`  
**页面：** `classroom-content-analysis`（`reportType=A` + `reportSubType=A2`）  
**Figma：** `vmbLwcwclGPoT3fWJWv7de` / `8674:27619`  
**归档日期：** 2026-08-26

---

## 1. 原始需求（初版）

1. 独立 View / mapper / mock / 章节数据；Registry `A2 → ReportTypeA2View`；A1 零回归。
2. **目录与 A1 不同**：按权威 TOC 建树，禁止套用 A1 章节。
3. **Mock 与 A1 解耦**：`classroom-content-analysis-a2-report.ts` + `type-a2-chapters.ts` + `classroom-content-analysis-a2.mock.ts`；禁止 `extends TypeA*` / `summaryBlocks` / A1 mock。
4. 先拆稿再实现；样式组件可复用 A1 原子，**不得影响 A1 默认行为**。
5. 时间锚点与 A1 一致（可点击跳转视频）。
6. 暂无接口：全 mock；空字段 `--`。
7. 每完成一小节对照 Figma 审查并记录（禁止整批堆完再对稿）。

### Hero 白名单（稿 `8674:27621`）

**有：** 标题、reportTag、分享、科目、年级、教材章节、课堂时长、**模板样式：A2**、总评分、评分等级。  
**无：** 课题 `topic`、`overallSummary`、特别声明（稿面无）。

---

## 2. Revision 02 · 高保真还原

| 原则 | 说明 |
|------|------|
| 稿有什么 mock 什么 | 文案/行数/列宽/卡数以 Figma 为准 |
| 稿没有的不做 | 禁止 A1 独有字段/小节 |
| 同样式才复用 A1 | 异构必须 `ReportA2*` |
| 一小节一审查 | 实现 → 截图 → `archive` 记录 |

主要异构模块：1.1 段落、1.2 亮点表、1.3 不足分类卡、3.1 知识矩阵、3.2 编号面板、3.5 案例九宫格、3.6.1 布鲁姆统计、3.6.2 问题链栈、10.1 补偿标记等。

---

## 3. Revision 03 · 条件渲染与样式补全（2026-08-26 会话）

### 3.1 条件渲染（7 个 flag）

| Flag | 位置 | 正常态 | 关闭态 |
|------|------|--------|--------|
| `hasKnowledgeRelationTypes` | 3.2 板块二标题旁 Switch | 编号明细 | 「知识点之间无明确关系类型可分析」 |
| `hasThinkingSpanEvaluation` | 3.2 板块三 | 编号明细 | 「暂无足够依据进行思维跨度合理性评估」 |
| `hasGradeCognitionFit` | 3.2 板块四 | 编号明细 | 「暂无足够依据评估与学段认知规律的符合度」 |
| `hasExperimentActivity` | 3.4.2 小节标题 | 表格数据 | 仅「【无实验/活动】」（**不显示表头**） |
| `hasProblemChain` | 3.6.2「问题链设计逻辑分析」标题 | 基本情况+分节+评价 | **表格常显**；逻辑区「不存在问题链条，本模块不适用」 |
| `hasSummaryTeaching` | 5.0 小节标题 | 展示 5.1–5.3 | 隐藏 5.1–5.3；章末 20px 加粗「本节课无总结性教学分析」 |
| `importHasCoreQuestion` | 5.3 小节标题 | 分析表 | 「导入环节未提出核心问题，本模块不适用」 |

**开发探针：** 各条件标题旁 `ReportA2FlagSwitch`；接入 API 后 `:show-flag-probes="false"` 隐藏。

### 3.2 布局与样式

- **末行孤儿卡撑满：** 1.3 / 3.1 / 3.5 / 3.6.1 层次卡等 2 列网格，`grid-last-orphan-span`。
- **3.6.1「合计」：** 在网格外，`width: 100%`（非 50% 宽、非并入网格）。
- **蓝框卡标题条：** 固定 `height: 50px` + `box-sizing: border-box`（mixin `a2-card-title-head`）。
- **3.1 第三部分：** 浅蓝底分点卡片「综合结论」。
- **5.0 / 6.2：** `a2BulletList variant: panel`。
- **标题间距：** L1→内容 40px；L2/L3→内容 20px；章间距 40px。

### 3.3 文案

- 3.2 卡片字段「改进建议」→「**建议**」。
- 3.6.2 / 5.1 / 5.2 表头「观察内容」→「**观察内容（从音转文提取）**」。

---

## 4. A2 权威目录（摘要）

```
Hero
一、课堂整体总结（1.1–1.3）
二、新课导入（2.1–2.7）
三、新知讲授/探究（3.1–3.7，含 3.4.x / 3.6.x）
四、课堂练习与反馈（4.1–4.3）
五、课堂小结（5.0–5.3）
六、学生学情综合诊断
七、课堂环节时间分配
八、教案预设与课堂实际对比分析
九、综合对比优秀课例
十、本课堂评分评级（10.1）
```

---

## 5. 验收（终态）

- [x] `reportSubType=A2` → `ReportTypeA2View`
- [x] A1 零回归；A2 独立类型/mock
- [x] 条件渲染 7 路 + 标题旁 Switch 可验
- [x] 结构单测 `classroom-content-analysis-a2-structure.spec.ts` 通过
- [ ] 接口接入后 mapper 填 `report.flags` 并关闭探针
- [ ] 全页像素级与 Figma 终验（浏览器并排）
