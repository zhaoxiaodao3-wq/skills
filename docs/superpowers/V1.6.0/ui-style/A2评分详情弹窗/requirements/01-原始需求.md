# A2 评分等级详情弹窗 · 原始需求

**日期：** 2026-08-27  
**Figma：** [评分等级计算 `8674:32751`](https://www.figma.com/design/vmbLwcwclGPoT3fWJWv7de/%E6%98%8E%E7%9D%BF%E5%BC%80%E5%8F%91%E7%A8%BF-%C2%B7%E5%9B%A2%E9%98%9F%E7%89%88?node-id=8674-32751&m=dev)  
**fileKey：** `vmbLwcwclGPoT3fWJWv7de` · **nodeId：** `8674:32751`

---

## 用户原话

> A2 报告中，点击「查看详情」按钮会弹出这个弹窗，数据你都帮我 mock，一比一还原设计稿，然后查看详情按钮帮我优化一下样式，hover 态点击态等。

---

## 范围

1. **弹窗「评分等级计算 / 评分等级详情」**（Figma 宽 1000、高约 4504 的长内容区）
   - 标题栏 + 关闭
   - 五个维度区块（稿内自上而下：维度五→一）
     - 评分项表（编号 / 评分项 / 分值规则 / 得分）
     - 总积分区间 ↔ 档位 ↔ 档位系数表
     - 维度小计说明（蓝底文案区）
   - 维度一额外：补偿检查表（条件 / 实际 / 结果，含对勾/叉图标）
   - 底部「总分与等级」：总分小计、时长系数、最终总分、判定等级徽章
2. **触发：** 第十章 `ReportA2ScoreSummary`「最终总分」行上的「查看详情」按钮（已有 `emit('detail')`，尚未接线弹窗）
3. **按钮态：** 默认 / hover / active（按 Figma 或与主色 `#027AFF` 交互规范对齐）
4. **数据：** 全部 mock，结构可后续接 API；与第十章汇总表数字可对齐 mock 已有值（如最终 81.10 / 等级 B）

## 非范围（本需求不做）

- PDF 静态 HTML 中的「查看详情」（PDF 特规仍禁止）
- 真实后端评分算法 / 接口联调
- 改第十章评分汇总表本身字段逻辑（仅接弹窗 + 按钮样式）

## 现状探查（只读）

| 项 | 说明 |
|----|------|
| 按钮 | `ReportA2ScoreSummary.vue` 已有 `.cca-a2-score-summary__detail-btn`，仅 default，无 hover/active |
| 接线 | `ReportTypeA2View` **未** `@detail` 打开弹窗 |
| 弹窗组件 | 尚无评分详情 Dialog；可参考 `ReportTimeVideoDialog` + Element Plus `el-dialog` |
| Mock | `mock/a2-data/chapter-rest.ts` 中 `showDetailButton: true` |
| Token | 与 A2 报告一致：`$cca-brand` `#027AFF`、灰阶、蓝浅底等 |

## Figma 结构摘要（MCP）

- 根 Frame `8674:32751` 宽 1000
- 顶栏：标题「评分等级计算」+ 关闭
- 内容滚动区：维度五/四/三/二/一 + 总分与等级
- 维度一含「补偿检查」子表
- 关键色：`#333` `#555` `#777` `#F2F3F5` `#F3F9FF` `#027AFF` `#00B42A` / 徽章绿边
