# A2 评分等级详情弹窗 · 对/错图标补缺

**日期：** 2026-09-10
**关联模块：** `ui-style/A2评分详情弹窗`（DELIVERED 追加）
**Figma：** [评分等级计算弹窗 `8674:33290`](https://www.figma.com/design/vmbLwcwclGPoT3fWJWv7de/%E6%98%8E%E7%9D%BF%E5%BC%80%E5%8F%91%E7%A8%BF-%C2%B7%E5%9B%A2%E9%98%9F%E7%89%88?node-id=8674-33290&m=dev)
**关键节点：** `8674:33311` (`mr-general-check circle`) · `8674:33320` (`mr-general-close circle`)
**fileKey：** `vmbLwcwclGPoT3fWJWv7de`

---

## 用户原话

> 这个是评分等级计算弹窗的内容，我需要你帮我确认对和错这两个图标，我看页面上跟设计稿不一致，首先确认项目中有没有相同的，有就用，没有就用 svg 画出来

## 现状探查（只读）

| 项 | 结果 |
|---|---|
| `src/icons/svg/` | 仅 `close.svg`（关闭 X 符号），**无**对/错图标 |
| `ReportA2ScoreDetailDialog.vue` 内联 SVG | 4 段 inline SVG（行 168-198 + 207-237），绿圆勾 + 红圆叉 |
| 全局复用 | `#00B42A` / `#F53F3F` 仅在该 dialog 内出现，无其它文件复用 |
| `SvgIcon` 公共组件 | `src/components/SvgIcon/index.vue` 接受 `src` URL + 可选 `color`，与本项目惯例一致 |
| `main.ts` 注册 | `<SvgIcon>` **未**全局注册（需局部 import），与已查 23 个使用方一致 |

### 当前实现 vs Figma 真实数据

| 图标 | 颜色 Figma | 颜色 当前 | 结构 Figma | 结构 当前 |
|---|---|---|---|---|
| 对（check circle） | `#00B42A` | `#00B42A` ✅ | 单 path fill-rule 一体 | 双 path（circle + stroke 勾）❌ |
| 错（close circle） | `#FF2A2A` | `#F53F3F` ❌ | 单 path fill-rule 一体 | 双 path（circle + stroke 叉）❌ |

## 范围

1. **新建** `src/icons/svg/check-circle.svg`：Figma 真实 SVG（绿 `#00B42A`，单 path）
2. **新建** `src/icons/svg/close-circle.svg`：Figma 真实 SVG（红 `#FF2A2A`，单 path）
3. **改** `src/pages/analysis-web/ai-teaching-diagnosis/classroom-diagnosis/components/ReportA2ScoreDetailDialog.vue`：
   - 局部 import `SvgIcon` 组件 + 两个 SVG URL
   - 删除 4 段 inline svg
   - 替换为 `<SvgIcon :src="..." :size="20" />`（行 20 大）/ `<SvgIcon :src="..." :size="14" />`（行 14 小）
   - 同步调整 SCSS：删除 `&__icon--pass/fail` 类，保留 `&__icon` 容器与 `&__icon-dash` 兜底短横线
4. 文档落盘：`specs/01-dev-spec.md` 末尾追加 `§10 对/错图标补缺`
5. 新建 `plans/02-icons-fix.md` + skill 路由标注
6. 新建 `archive/A2-score-detail-dialog-icons-fix-delivered.md`

## 非范围（本需求不做）

- 不改 mock 数据结构（`mock/a2-data/score-detail-dialog.ts` 字段 OK）
- 不改 `ReportA2ScoreSummary.vue` 按钮态
- 不改 `ReportA2ScoreDetailDialog.vue` 其它视觉（顶栏、表头、表体、徽章）
- 不抽 dialog 内其它内联 SVG（如关闭 X）
- 不接真实 API

## 验收（spec §10 增补）

- [ ] 弹窗内"对"图标视觉与 Figma `8674:33311` 一致（颜色 + 单 path 结构）
- [ ] 弹窗内"错"图标视觉与 Figma `8674:33320` 一致（颜色 `#FF2A2A` 修正 + 单 path 结构）
- [ ] 14px（判定徽章）/ 20px（结果列）两个尺寸均显示正常
- [ ] `resultKind` 为 `pass` / `fail` 之外的场景仍走"-"兜底
- [ ] 不影响 dialog 其它区域（顶栏、表头、表体、徽章、关闭 X）
- [ ] 公共 SVG 在 `src/icons/svg/` 下，命名 `check-circle.svg` / `close-circle.svg`，与 `close.svg` 同级
