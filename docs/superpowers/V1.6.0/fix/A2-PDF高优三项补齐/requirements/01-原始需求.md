# 原始需求：A2 PDF 高优三项补齐

**类型：** fix · **档位：** 标准  
**用户确认：** 补修审查清单中优先级高的三项  

1. 3.6.2 `basicInfo` → 拆成「是否存在问题链 / 问题总数」（对齐 Web）  
2. 3.3 补绑 `effectivenessSummary`（教学行为有效性总结）  
3. 10.1 时长系数 hint 按 `classDurationRaw` 动态计算（对齐 `buildDurationCoefficientHint`）  

**范围：** `src/report/report/A2/ClassroomContentAnalysisReportA.html`（必要时补 fixture）  
**方案：** 模板内 Thymeleaf 裁剪，不改后端 VO  
