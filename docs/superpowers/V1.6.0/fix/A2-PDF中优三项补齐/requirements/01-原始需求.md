# 原始需求：A2 PDF 中优三项补齐

**类型：** fix · **档位：** 标准  
**用户确认：** 中等优先级三项一并补修  

1. Bloom 层级名去掉前缀 `^\d+\.`（如 `1.记忆`→`记忆`）  
2. 表「评价」列对齐 Web `joinEval`（`string[]` 用换行拼接）  
3. 板块二/三、3.3 教案重难点序号补零为 `01`/`02`…  

**范围：** `ClassroomContentAnalysisReportA.html` + 本地预览 runtime（`listJoin` / `instanceof`）+ fixture  
**方案：** 模板内 Thymeleaf 裁剪，不改后端 VO  
