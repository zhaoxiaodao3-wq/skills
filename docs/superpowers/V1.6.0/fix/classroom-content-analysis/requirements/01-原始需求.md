# 原始需求

恢复 A2 报告分享链接路由拼写。

- **背景**：commit `6a2842f6` 在 `classroom-content-report-registry.ts` 给 A2 加了独立的 `share.path = 'analysis-teaching-a2'`，与 A1 / B1 / B2 的 `FAMILY_SHARE.{A|B}` spread 写法不一致。
- **期望**：A2 与 A1 共用 `analysis-teaching-a` 路径，由目标跳转页根据 `caseBasicInfo.reportType + reportVersion` 判断渲染 A1 / A2。
- **范围**：仅改 `classroom-content-report-registry.ts` 1 行；`shareType`、view、mapPayload、其他家族不动。

---

## Phase 5 · A2 报告 10.1「查看详情」弹窗 × evaluationResult 字段对接（2026-09-09）

### 背景

A2 报告第 10.1 节（"评分汇总表"）的"查看详情"按钮 → 打开弹窗 `ReportA2ScoreDetailDialog`（位于 `src/pages/analysis-web/ai-teaching-diagnosis/classroom-diagnosis/components/ReportA2ScoreDetailDialog.vue`）。

当前弹窗数据用 `A2_SCORE_DETAIL_DIALOG_MOCK` 占位（`mappers/classroom-content-analysis-a2.mapper.ts:1572, 1591`，注释「G05：评分详情弹窗暂用既有 mock」）。需要改为基于接口 `evaluationResult` 字段动态生成。

### 接口字段来源

- 接口：`/analysis/v2/teachingDiagnosis/getDiagnosisResult`（`service/analysisReport.ts:1264-1266`）
- 字段路径：`response.evaluationResult`（A2 报告独有；位于 `caseBasicInfo.aReport` VO）
- 字段文档：`课堂评估-final.json`（2026-08 后端接口文档）

### 期望

`mapA2ReportToClassroomContentPayload` 输出的 `payload.report.scoreDetail` 从 `A2_SCORE_DETAIL_DIALOG_MOCK` 切换为 `mapEvaluationResultToScoreDetail(vo.evaluationResult)`。

`evaluationResult` 为 null/undefined 时仍 fallback 到 mock（保持原行为）。

### 范围

- 改：`mappers/classroom-content-analysis-a2.mapper.ts`（新增 mapper 函数 + 改两处赋值）
- 不改：mock / 弹窗组件 / 后端 / 路由 / 样式

### 决策记录（用户 P1 确认 2026-09-09）

| # | 决策点 | 决策 | 实施落点 |
|---|--------|------|---------|
| A | 档位区间表（3 列：区间/档位/系数）| 前端写死 3 套常量 | 复用 `TIER_ROWS_4PT` / `TIER_ROWS_3PT` / `TIER_ROWS_KNOWLEDGE`（按 `fullScore` 选） |
| B | 维度底部总结 + 补偿小计 + 补偿底部说明 | 前端拼文案，后端有素材 | 5 个文案模板（subtotalLine / gradeLine / scoreLine / footerGradeLine / footerScoreLine + judgmentLabel + judgmentUpgradeNote）|
| C | 补偿模块是否展示 | 按 `compensationCheck` 字段是否非空 | **5 个维度都判断**（修正原"仅知识落实度"误判）|
| D | 4 列表头（条件/具体要求/检查结果/说明）| conditionList 字段名映射 | 写死 1 个常量 `['条件', '具体要求', '检查结果', '说明']` |
| E | 课堂时长行 | 不推导区间，直接拼 | 用 `classDurationRaw` + `classDurationCoefficient` + 现有 `buildDurationCoefficientHint` 拼 |
| F | evaluationResult 字段 | 已在 A2 mapper 中使用 | 直接用 `vo.evaluationResult` |
