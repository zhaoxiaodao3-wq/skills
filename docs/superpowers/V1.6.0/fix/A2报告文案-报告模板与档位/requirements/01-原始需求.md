# 原始需求：A2 报告文案（报告模板与档位）

**类型：** fix · **档位：** 标准  
**用户确认（P1）：** H5 已是「报告模板」，只改 Web + PDF；「挡位→档位」按探查方案（仓库已无「挡位」则再扫确认）

## 范围

1. Web `ReportA2HeroHeader`：`模板样式` → `报告模板`  
2. PDF `ClassroomContentAnalysisReportA.html`：同上；生成器 `gen-ccar-a2-static-html.mts` 同步  
3. **不改 H5**；不改 B2  
4. 全仓再扫「挡位」；若仍为 0 处则无实现改动  
