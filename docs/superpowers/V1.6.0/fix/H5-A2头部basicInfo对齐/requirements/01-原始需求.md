# H5-A2 头部 basicInfo 对齐 · 原始需求

**日期：** 2026-09-09  
**档位：** 标准  
**来源：** H5 A2 全量对接后，封面头部信息应与 A1 一致，使用分享接口 `basicInfo`

## 背景

- 旧模块 `api-adapter/H5-A2报告接口对齐` 已 DELIVERED
- A2 `mapCoverAndOverview` 已部分读 `basicInfo`，但字段别名与 A1 不一致（缺 `analysisName` 等），头部易空或错落到报告体

## 要什么

1. A2 **报告头部封面卡片（Cover）** 字段取值与 A1 对齐：**优先 `envelope.basicInfo`**
2. 别名顺序与 A1 `buildAReportView` 一致

## 不做

- 改 H5 视觉样式 / 布局
- 将总分、评分等级改为从 `basicInfo` 读取（A1 也在报告体）
- 重做 overview 章节 UI

## 权威对照

- A1：`E:\code\H5\src\pages\share\analysisTeachingA\report-view.ts` → `buildAReportView`
- A2：`E:\code\H5\src\pages\share\analysisTeachingA2\adapters\mapA2ToView.ts` → `mapCoverAndOverview`
