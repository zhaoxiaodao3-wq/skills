# H5 A2 无用逻辑清理 · 原始需求

**日期：** 2026-09-09  
**档位：** 标准  
**来源：** 用户「H5 中清理 A2 中的无用逻辑文件等」；P1 确认三项

## 要什么

1. 删除未引用的 `styles/a2-text-wrap.scss`（及空 `styles/`）
2. 移除 overview 已隐藏字段的类型与映射：`topic` / `totalScore` / `gradeCode` / `gradeLabel`
3. `mapA2ToView.ts` 中仅模块内使用的 helper 去掉 `export`（行为不变）

## 不做

- 不删章节组件、不改 10.1 / 其它章节业务映射
- 不重构 `highlightLead`
- 不改 `extractA2Report` 对旧 `scoring` 的形状识别

## 实现仓

- `E:\code\H5\src\pages\share\analysisTeachingA2\`
