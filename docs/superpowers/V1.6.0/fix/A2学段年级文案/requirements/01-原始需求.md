# A2学段年级文案 · 原始需求

**日期：** 2026-09-15  
**模块：** `fix/A2学段年级文案`  
**档位：** 标准  
**类型：** fix（文案纠错）

---

## 背景

A2 报告「板块四 · 与学段认知规律的符合度」下方 meta 行当前展示为「学段与学年」，且多处逻辑把接口/模板中的「年级」强制改成「学年」。产品要求改为 **「学段与年级」**。

## P1 已确认

- **方案 A**：Web + H5 + PDF（muban + 本仓 PDF 源同步）
- 入参仍可识别「学段与学年」或「学段与年级」；**展示统一为「学段与年级：…」**
- 去掉一切「年级 → 学年」的替换方向

## 范围

### 必做

1. **Web**  
   - `normalizeCognitiveFitMetaLine`：输出前缀改为 `学段与年级：`；不再把「学段与年级」替换为「学段与学年」  
   - 同步单测、mock、类型注释中的示例文案  
2. **H5**  
   - `mapA2ToView.ts` 同名规范化逻辑与组件注释（如有）  
3. **PDF**  
   - `E:\code\muban\analysis-service\src\main\resources\lessonTemplates\A\A2\ClassroomContentAnalysisReportA.html`：Thymeleaf 去掉「年级→学年」replace，展示前缀与占位改为「学段与年级」  
   - 本仓 `src/report/report/A2/ClassroomContentAnalysisReportA.html` 同源修改，避免分叉  

### 不做

- 改板块四其它分析正文内容  
- 改接口字段契约（仅展示层规范化）  
- 改表格列宽 / 序号对齐等无关样式  

## 验收要点

1. Web / H5 板块四 meta 显示「学段与年级：…」，不再出现「学段与学年」作为展示前缀  
2. 接口原文含「年级」或「学年」时，规范化后均以「学段与年级」展示  
3. muban 与本仓 A2 PDF 模板占位与 `metaDisp` 均为「学段与年级」  
4. 相关单测 / mock 断言与示例同步通过  
