# A2 · 3.3 教案重难点参考标题加粗 · 原始需求

**日期：** 2026-09-08  
**档位：** 标准  
**类型：** fix

## 要什么

在 **3.3 教学重难点突破分析 → 教案重难点参考信息** 下，两条小点的**标题前缀加粗**（与 mock 的 `leadLabel` 一致）。

接口 `planReference` 为整串，例如：

- `教案中标注的教学重点：①……`
- `教案中标注的教学难点：①……`

需用固定前缀 / 正则拆成 `leadLabel` + `leadContent`，再走现有 `ReportA2NumberedPanel` 的 lead 加粗样式。

## 不做

- **维度三**高低阶结构合理性：标题与内容换行 —— **取消，不改**
- 3.3 其它区块（重难点卡片网格、有效性总结等）
- 接口字段语义变更、H5 视觉重做

## 背景

- Web 真映射现用 `mapNumberedFromStrings`，整行进 `content`，前缀无法加粗
- 约定见：`api-adapter/A2报告接口字段对齐/archive/spike-G01-G17-Mock与接口差异明细.md` · **G08**

## 改动面（预期）

- 主：`classroom-content-analysis-a2.mapper.ts`（拆条解析 + 单测）
- 组件样式已有 `__lead-label { font-weight: 600 }`，一般不必改 Vue
