# A2-3.3教案重难点标题加粗 · 交付归档

**归档类型：** fix 交付快照  
**归档日期：** 2026-09-08  
**版本：** V1.6.0  
**Requirement:** [../requirements/01-原始需求.md](../requirements/01-原始需求.md)  
**Spec:** [../specs/01-dev-spec.md](../specs/01-dev-spec.md)

## 改动摘要

3.3「教案重难点参考信息」不再把 `planReference` 整串塞进 `content`；按固定前缀正则拆成 `leadLabel` + `leadContent`，走 numbered panel 既有加粗样式。维度三未改。

## 改动文件

| 操作 | 路径 |
|------|------|
| 改 | `src/pages/analysis-web/ai-teaching-diagnosis/mappers/classroom-content-analysis-a2.mapper.ts` |
| 改 | `src/pages/analysis-web/ai-teaching-diagnosis/mappers/classroom-content-analysis-a2.mapper.spec.ts` |

## 验收结果

- [x] 标准两行前缀拆条 + lead 加粗（组件既有 `__lead-label`）
- [x] 中英文冒号
- [x] 无前缀下标兜底
- [x] 维度三无改动
- [x] 单测 14 passed（含本模块新用例）

## 一致性自检

| 检查项 | 结果 | 证据（路径或说明） |
|--------|------|-------------------|
| 空态 vs 有数据 | 通过 | `null`/`[]` → `items=[]`；有数据 → 固定两槽 leadLabel |
| 常量/mock/真数据 | 通过 | leadLabel 文案与 mock / spike G08 一致；解析测覆盖真接口样例 |
| 多入口 | N/A | 仅 Web A2 mapper 接线；本期不改 H5 |
| 失败/缺省 | 通过 | 无前缀回填固定标题；缺槽 `leadContent='--'` |

## 还原度自检

不适用：无 Figma / 非 UI 新样式；沿用 `ReportA2NumberedPanel` lead 样式。

## Harness 闭环

- [x] validate 开发前已跑
- [x] archive 交付快照已写
- [x] validate 交付后已跑
