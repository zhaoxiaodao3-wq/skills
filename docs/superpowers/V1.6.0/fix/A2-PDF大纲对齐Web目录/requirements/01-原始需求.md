# A2-PDF大纲对齐Web目录 · 原始需求

**日期：** 2026-09-17  
**分类：** fix  
**档位：** 标准

## 需求原文

网页 PDF 预览（`ai-teaching-analysis-report`）右侧「大纲」与 Web 报告页（`classroom-content-analysis`）目录保持一致；点击大纲项应滚动到 PDF 对应位置。按既有习惯：**优先只动 PDF 模板，不改前端预览页**。

## 背景结论（探查）

- 右侧大纲来自 `pdf.js getOutline()` ← Playwright `setOutline(true)` 按 HTML **标题标签**生成书签，不是解析 TocA 目录页。
- TocA 静态目录页文案已与 Web TOC 对齐；不一致主因是正文大量 **h4**（卡片/面板标题）被写进书签，以及 Hero「课堂基本信息…」目前是 `<p>` 未进书签。
- 点击跳转依赖书签 dest，模板标题层级正确后无需改 `AppViewerAnalysis`。

## 范围

- A2 PDF 模板（muban `lessonTemplates/A/A2` + 本仓 `src/report/report/A2` 同源同步）
- 不改 `ai-teaching-analysis-report.vue` / `AppViewerAnalysis`（除非模板验证后仍不对齐）
- 不改 Web/H5 Vue 报告侧栏逻辑
