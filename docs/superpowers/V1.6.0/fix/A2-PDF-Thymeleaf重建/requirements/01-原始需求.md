# A2-PDF-Thymeleaf重建 · 原始需求

**日期：** 2026-09-11
**模块：** `fix/A2-PDF-Thymeleaf重建`
**档位：** 标准
**类型：** fix
**关联文件：**
- 目标：`src/report/report/A2 copy/ClassroomContentAnalysisReportA2.html`（183KB / ~2900 行 / 当前**无** `th:*`）
- A1 权威参考：`E:/code/muban/analysis-service/src/main/resources/lessonTemplates/A/A1/ClassroomContentAnalysisReportA.html`（113KB / 132 th:text / 24 th:each / 46 th:if / 23 th:with）
- 前端规范：`src/report/FRONTEND_THYMELEAF_GUIDE.md`（A1 字段映射 §7；坑点见 §5.5 11 条硬性语法限制）
- 后端规范：`src/pages/analysis-web/ai-teaching-diagnosis/template-fix/TEMPLATE_CHECKLIST.md`（13 节坑点）
- Web 端字段源：`src/types/teaching-diagnosis-a2-report-vo.ts`（`PostClassReportA2VO` flat 顶层结构）
- Web 端格式化函数：`src/pages/analysis-web/ai-teaching-diagnosis/mappers/classroom-content-analysis-a2.mapper.ts`（`formatScoreOverFull` / `parseClassDurationMinutes` / `buildDurationCoefficientHint` / `mapFlags`）
- 空态文案：`src/pages/analysis-web/ai-teaching-diagnosis/classroom-diagnosis/constants/a2-report-messages.ts`

---

## 用户原话

> 这个是后面要提供给后端的html文件，用于生成PDF，现在的话还差一步处理，就是插入themleaf语法，具体的字段路径参考web版本，web已经全部对接好接口了
>
> A2 报告的，可以参考一下 A1 已经做好的，然后项目里面应该有直接写这种模板语法的总结文件，会有一些坑，可以参考下
>
> 你知道的，我的宗旨是一次把事情办好，不要让我过多的去重复修改，争取一次性把事情做好，可以参考 thymeleaf 这个 skill
>
> 最终目标就是不影响任何原来的样式逻辑，把语法接入模板，要拆细分任务一项一项执行，因为内容太多了，生成一个全新的任务执行

> **后续补充（2026-09-11）：** 忽略旧版计划，重新计划

---

## 关键决策

1. **根变量** = `aReport`（flat 顶层结构，**不**走 A1 的 `aReport.contents.*`）
2. **章节根用 `th:block` + 单变量 `th:with` 短别名**（对齐 A1 的 `ov`/`li`/`nk` 等风格）
3. **只接 HTML 已有 mock 位**（FRONTEND_GUIDE §0 核心原则）；VO 多出字段**不接**、**不加 DOM**
4. **空态显示「暂无数据」**（对齐 `a2-report-messages.ts`；保留 thead）
5. **保留标签内默认 mock 文字**（设计稿预览用；剥 `th:*` 后仍可读）
6. **不动 `<head>` 内 `<style>` / `ccar-*` 类名 / DOM 结构 / `<colgroup>` 列宽**
7. **关键字段加 `data-field` 标注**
8. **执行方式** = Inline（主线程串行 8 task，每 task 完成后立即自检）

---

## A2 HTML 当前结构（11 section / 32 subsection）

| Section ID | 标题 | 子节数 |
|------------|------|--------|
| `section-hero` | 课堂基本信息 | 0 |
| `section-summary` | 一、课堂整体总结 | 3（1.1 总结 / 1.2 亮点 / 1.3 不足） |
| `section-import` | 二、新课导入 | 7（2.1~2.7） |
| `section-new-knowledge` | 三、新知讲授 | 14（3.1 / 3.2 / 3.3 / 3.4.1~3 / 3.5 / 3.6.1~2 / 3.7） |
| `section-practice` | 四、课堂练习 | 3（4.1~4.3） |
| `section-classroom-summary` | 五、课堂小结 | 4（5.0~5.3） |
| `section-learning` | 六、学生学情 | 2（6.1~6.2） |
| `section-time` | 七、时间分配 | 1（7.1） |
| `section-plan-vs-actual` | 八、教案对比 | 1（表格 + 总结卡） |
| `section-excellent` | 九、优秀课例 | 1（表格 + 说明卡） |
| `section-scoring` | 十、评分评级 | 1（10.1 评分汇总表） |

---

## 范围

### 必做

1. **A2 HTML 顶部加 `xmlns:th`**（`<html lang="zh-CN" class="ccar-content-doc" data-ccar-print-cards="html" xmlns:th="http://www.thymeleaf.org">`）
2. **11 section 全部按 mock 已有位接 Thymeleaf**，分 8 个 task 执行
3. **章节根用 `th:block` + 单变量 `th:with` 局部别名**（不写多变量；不用 `?:` 内嵌）
4. **空态兜底**：每节空列表 / 空段落 → 通用「暂无数据」或 `a2-report-messages.ts` 特定空态文案
5. **7 个 renderFlag 条件渲染**（与 web 端 `mapFlags` 对齐）
6. **保留标签内默认 mock 文字**
7. **关键字段加 `data-field`**

### 不做

- 不接 VO 多出但 HTML 未展示的字段
- 不新增任何 DOM / section / 块
- 不搬 Vue computed 逻辑到模板
- 不动 `<head>` 内 `<style>`（CSS 全部保留）
- 不动 `<colgroup>` 列宽
- 不动封面/目录 PDF HTML
- 不接 `evaluationResult.scoreItems[]` / `dimensionList[].compensationCheck.conditionList[]`
- 本期不写本地 Thymeleaf 渲染器 / `pnpm gen:a2:pdf` / FRONTEND_GUIDE §23

## 验收

- [ ] `<html>` 头部含 `xmlns:th="http://www.thymeleaf.org"`
- [ ] 11 section 全部 HTML 已有 mock 展示位均有 `th:*`
- [ ] 章节根用 `th:block` + **单变量** `th:with`（不写多变量 / 不内嵌 `?:`）
- [ ] 字段路径全部落在 `PostClassReportA2VO`（无编造）
- [ ] 空态文案与 `a2-report-messages.ts` 一致
- [ ] 7 个 renderFlag 条件渲染与 `mapFlags` 一致
- [ ] 标签内默认 mock 文字保留
- [ ] 无 `th:each` 旁残留静态兄弟
- [ ] 关键字段加 `data-field` 标注
- [ ] `<head>` 内 `<style>` / CSS 类名 / DOM 结构 / `<colgroup>` 列宽**完全未动**
- [ ] 自检脚本 grep 全部通过（详见 spec §10）
- [ ] `pnpm harness:check` 无本模块相关警告
- [ ] `pnpm harness:status` 阶段 = `DELIVERED`
