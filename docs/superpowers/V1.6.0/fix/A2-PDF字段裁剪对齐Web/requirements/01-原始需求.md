# 原始需求：A2 PDF 字段裁剪对齐 Web

**类型：** fix  
**档位：** 标准  
**P1 确认：** 方案 A（模板内 Thymeleaf 裁剪，对齐 Web mapper）  
**范围路径：** `src/report/report/A2/`（主文件 `ClassroomContentAnalysisReportA.html`）

## 用户原述

A2 PDF 模板中以下模块对数据的处理应与 Web 对齐（Web 有裁剪/格式化，非直接绑字段）：

1. 逻辑评价深度分析 · 板块一、板块四  
2. 3.3 教案重难点参考信息（两个点）  
3. 维度三 · 高低阶结构合理性  
4. 问题链 · 递进路径总结  
5. 3.6 最下方整体评价与优化建议  
6. 10.1 表得分列展示满分（如 `29.4/35`）  
7. 课堂时长 T 格式对齐 Web  
8. 最终得分后缀括号对齐 Web  

## 方案

- **A（已选）：** 仅在 A2 PDF HTML 模板用 `th:with` / `#strings` 复刻 Web `classroom-content-analysis-a2.mapper.ts` 主路径裁剪规则  
- **B（未选）：** 后端 PDF 模型预裁剪  

## 非目标

- 不改 Web / H5 / 后端 VO  
- 不追求与 mapper 正则 100% 边角等价；主路径对齐即可，无法拆解时降级为「去前缀后的正文」
