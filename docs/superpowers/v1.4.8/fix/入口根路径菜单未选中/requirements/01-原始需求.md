# 入口根路径菜单未选中

## 需求描述

访问测试环境入口 `https://test.mirayai.com:31594/classroom-app/`（对应应用内路由 `/`）时，侧边栏不再自动选中/跳转到原先默认菜单项；以前会自动选中。

## 现象

- 打开 `/classroom-app/` 后停在空根路径页
- 菜单无高亮
- 非重定向到业务首菜单

## 初步判断

与近期 fix「本地菜单跳转卡顿」引入的 `:skip-auto-navigate="loading"` 竞态相关：菜单就绪时若 `loading` 仍为 true 则跳过自动导航，且之后未重试。

## 来源

用户 `/harness` 反馈；入口 URL：https://test.mirayai.com:31594/classroom-app/
