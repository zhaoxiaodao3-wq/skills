# 原始需求

## 摘要

1. **5.1 导入分析**：补「是否提出核心问题」（`hasCoreQuestion`）；确认 `connectionToNewLesson`；查清是漏接还是从未接。
2. **5.2 新知教学**：三行内容格式应为  
   - 问题链：`hasQuestionChain` + `questionChainDetail`  
   - 案例：`hasCases` + `caseDetails`  
   - 探究：`hasInquiryActivity` + `activityDetails`  
   不对则修正。
3. **5.1 / 5.2** 两表：固定行保留；**空值不加占位符**（不用 `-`/`无`），接口返回什么展示什么；`has*` 拼接时详情可为空。
4. **范围：A/B 类都改**（B 类接口文档字段已核对齐全）。

## 排查结论

### 5.1

| 项 | A 类 | B 类 |
|----|------|------|
| 「是否提出核心问题」 | Vue mapper **从未接**（漏接）；HTML 已有 | **已接**（`formatIntroCoreQuestion`） |
| `connectionToNewLesson` | 已接 | 已接 |
| 空行 filter | `.filter(content !== '-')` 会藏行 | 同左 |

### 5.2

| 项 | 现状 |
|----|------|
| 共用 `buildNewKnowledgeTableRows` | 只读详情字段，不拼 `has*`；空用「无」 |

### B 类接口字段核对（`doc/1.4.7-api/V1.4.7_B类课后报告_前端接口文档.md`）

| 字段 | 7.1 introduction | 7.2 newKnowledgeTeaching |
|------|------------------|--------------------------|
| `hasCoreQuestion` / `coreQuestionContent` | ✅ | — |
| `connectionToNewLesson` | ✅ | — |
| `hasQuestionChain` / `questionChainDetail` | — | ✅ |
| `hasCases` / `caseDetails` | — | ✅ |
| `hasInquiryActivity` / `activityDetails` | — | ✅ |

**结论：B 类文档字段齐全，可与 A 一并改。**
