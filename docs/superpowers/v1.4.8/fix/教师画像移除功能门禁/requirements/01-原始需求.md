# 教师画像移除功能门禁

## 需求描述

教师画像已上线。配置注释写明上线后应从 `FEATURE_PAGE_CONTROL_LIST` 删除 path，并移除页面上的 `FeaturePageAccessGate`。

用户已将 `feature-page-access.ts` 中教师画像 path / 白名单注释掉；页面上仍包裹 `FeaturePageAccessGate`。

## 问题

确认是否还需移除组件包裹；若需要，一并清理门禁相关引用。

## 来源

用户 `/harness`：教师画像已上线，是否需要移除 FeaturePageAccessGate；已改 feature-page-access.ts 列表。
