# 原始需求

## 摘要

1. 隐藏 `src/pages/school/teacher-portrait/teacher-portrait/index.vue` 中的开发调试组件 `RoleDebugBar`。
2. 审查当前教师画像页各区块是否已对接真实接口（非仅 Mock）。

## 范围

- 页面：`src/pages/school/teacher-portrait/teacher-portrait/index.vue`
- 关联：左侧栏 / 右侧画像聚合模块的数据来源与 mock 开关
- 审查结论写入本需求交付物（spec / archive），不以新增对接为本次开发目标（除非审查发现阻断生产的硬编码）

## 验收期望

- 生产页面顶部不再展示角色/数据模式调试栏
- 页面默认走登录角色与真实 HTTP（`useMock` 默认 false）
- 交付一份按模块的接口对接审查结论
