# 教师画像左侧栏目身份审查

## 需求描述

审查教师画像「身份校验 → 左侧栏目」链路：不同账号登录是否能正确看到对应左侧栏目。

## 审查范围

- `teacher-portrait/index.vue` 左栏 `v-if` 三选一
- `resolveUserRole` / `getUserRoleFlags` / `useUserRole`
- `FeaturePageAccessGate` 白名单门禁
- RoleDebugBar / mock 是否干扰线上角色

## 来源

用户 `/harness`：审查教师画像身份校验，不同账号登录是否能正确看到对应左侧栏目。
