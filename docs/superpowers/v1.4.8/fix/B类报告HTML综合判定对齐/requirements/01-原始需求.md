# B类报告HTML综合判定对齐

## 需求描述

对照 `E:\code\muban\analysis-service\...\lessonTemplates\B\ClassroomContentAnalysisReportB.html` 与前端网页：5.5「综合判定」是否同源字段、内容是否与网页一致。

## 审查结论（待确认方案）

- **字段同源**：HTML 中综合判定逐条来自 `tb.objectiveAchievement.comprehensiveJudgment`（`tb` = teachingBehavior），与网页 mapper 的 `contents.teachingBehavior.objectiveAchievement.comprehensiveJudgment` 一致。
- **内容未对齐**：HTML 仍额外渲染 `totalObjectives` / `achievedCount` / `notAchievedCount` / `reasonAnalysis`；网页已收敛为仅 `comprehensiveJudgment`。前端仓库 `src/report/ClassroomContentAnalysisReportB.html` 与服务端模板目前同构，也偏旧。

## 来源

用户 `/harness`：检查 analysis-service B 模板 HTML，是否与网页对应、综合判定是否同一字段。
