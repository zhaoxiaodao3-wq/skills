# 原始需求

## 摘要

检查 `E:\code\muban\analysis-service\src\main\resources\lessonTemplates` 下根据网页转换的 A/B 报告模板，是否已同步「5.1 补核心问题行 / 5.2 has+详情拼接 / 空值不占位」的前端改动；缺则对齐模板。

## 对照基准

前端已交付：`fix/A类报告5.1和5.2展示补齐`（Vue mapper + `report-display-format`）。

## 只读检查结果（2026-07-14）

### A/`ClassroomContentAnalysisReportA.html`

| 项 | 模板现状 | 与 Vue 是否一致 |
|----|----------|----------------|
| 5.1「是否提出核心问题」 | **无该行**（时长→方式→衔接…） | ❌ 未同步 |
| 5.1 空值 | 空用 `-` / 改进建议用 `无` | ❌（Vue 已改为空串） |
| 5.2 问题链/案例/探究 | 仅绑 `questionChainDetail`/`caseDetails`/`activityDetails`，空用 `无` | ❌ 未拼 `has*` |

### B/`ClassroomContentAnalysisReportB.html`

| 项 | 模板现状 | 与 Vue 是否一致 |
|----|----------|----------------|
| 5.1「是否提出核心问题」 | **有行**，复杂 SpEL；双空时显示 `-` | 部分有，空值规则 ❌ |
| 5.2 has+详情 | 同 A，仅绑详情字段 | ❌ |
| 空值占位 | `-` / `无` | ❌ |

**结论：lessonTemplates 未随刚才改动更新（或仅 B 遗留旧版核心问题行，规则也未对齐）。**
