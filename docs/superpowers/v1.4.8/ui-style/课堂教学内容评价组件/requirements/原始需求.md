# 课堂教学内容评价组件开发需求文档

## 一、需求概述
本次提供的2份Figma设计稿链接暂无法直接解析内容，以下需求基于业务逻辑描述整理，所有视觉样式、布局细节均以对应设计稿节点为准。组件用于展示课堂教学内容相关的评价指标与占比数据，遵循项目统一的工程化、组件化开发规范，保障可复用性与可扩展性。

## 二、设计稿资源
所属项目：明餐开发稿 · 团队星版
共2份设计稿，分别对应组件不同状态：
1.  有数据状态：https://www.figma.com/design/vmbLwcwclGPoT3fWJWv7de/%E6%98%8E%E7%9D%BF%E5%BC%80%E5%8F%91%E7%A8%BF-%C2%B7%E5%9B%A2%E9%98%9F%E7%89%88?node-id=6696-14026&m=dev
2.  缺省状态：https://www.figma.com/design/vmbLwcwclGPoT3fWJWv7de/%E6%98%8E%E7%9D%BF%E5%BC%80%E5%8F%91%E7%A8%BF-%C2%B7%E5%9B%A2%E9%98%9F%E7%89%88?node-id=6696-20326&m=dev

## 三、组件状态定义
### 1. 有数据状态
为组件正常展示态，当评价相关数据可正常获取时渲染，完整展示所有评价指标、占比信息、统计图表、模块文案等全部内容。
### 2. 缺省状态
当核心数据缺失、接口异常无法获取评价数据时，切换展示缺省状态，视觉样式严格对齐缺省设计稿实现，不得自定义缺省样式。

## 四、数据展示规范
### 1. 占比类字段统一规则
- 组件内所有占比类数值，统一保留 **1位小数** 展示；
- 若原始值小数位数超过1位，直接截断第2位及后续内容，**不执行四舍五入** 处理；
- 优先级说明：即使设计稿示例中出现多位小数的占比数值，也一律以本规则为准进行处理展示，设计稿内的示例数值仅作布局参考。

### 2. 通用字段规则
设计稿中出现的其余所有展示字段、标签、辅助文案等，均按设计稿标注的格式与单位直接渲染，不做额外数据转换。

## 五、图表实现规范
1.  遵循项目统一技术规范，组件内所有统计图表模块**统一使用 ECharts** 进行实现，不得引入其他图表库。
2.  图表的配色、尺寸、坐标轴样式、图例、提示框等所有视觉细节，严格对齐设计稿效果。
3.  图表需支持响应式自适应，随容器尺寸变化自动调整，保证不同屏幕下展示完整、无溢出错位。

## 六、UI与开发通用规范
1.  **组件复用原则**：UI实现优先复用 Element Plus 组件库原生组件，可基于现有组件进行二次样式定制与功能封装，在严格对齐设计稿效果的前提下，最大化复用组件库能力。
2.  **样式隔离要求**：所有自定义样式必须做作用域隔离，仅对当前组件内部生效，不得污染全局样式、不得影响项目内其他页面与组件。样式隔离方案遵循项目现有工程规范。
3.  **响应式适配**：组件需具备完整的响应式适配能力，兼容项目覆盖的主流屏幕尺寸与分辨率，不同视口下布局合理、内容完整展示，无溢出、错位、遮挡、文字截断等视觉异常。
4.  **架构设计**：采用业务逻辑与视图分离的架构设计，结构清晰、职责单一，预留合理的扩展插槽、配置项与钩子，支持后续字段新增、样式调整与功能迭代；所有文件目录划分遵循项目现有前端工程规范，本需求不做强制指定。

## 七、Mock数据方案
开发阶段先行使用Mock数据联调，不依赖真实后端接口，接口就绪后可无缝切换。
1.  Mock数据与业务代码、视图逻辑分离，存放方式遵循项目现有Mock管理规范。
2.  需覆盖以下场景：
    - 全量正常数据场景，覆盖所有评价指标、占比与图表完整展示
    - 缺省状态场景
    - 占比数值含多位小数的截断验证场景
    - 单字段缺失、数据格式异常等边界场景

## 八、开发要求
1.  视觉还原严格对齐Figma设计稿的尺寸、色值、间距、字体与样式细节，全字段完整还原，保证1:1还原效果；
2.  占比数值处理逻辑准确，严格执行保留1位小数、不四舍五入的规则，设计稿示例数值仅作布局参考，不覆盖业务规则；
3.  图表统一使用ECharts实现，样式与交互符合设计稿要求，支持响应式自适应；
4.  兼容接口异常、字段缺失、数据格式异常等边界场景，缺省状态展示符合设计稿规范；
5.  优先复用 Element Plus 组件库能力，样式严格执行隔离规范，避免全局样式污染；
6.  完成多屏幕响应式适配验证，覆盖项目主流分辨率；
7.  组件设计遵循工程化、组件化规范，具备可扩展性，便于后续迭代维护。