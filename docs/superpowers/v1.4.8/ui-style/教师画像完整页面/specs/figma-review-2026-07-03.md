# 教师画像完整页面 Figma 审查报告

**审查日期：** 2026-07-03  
**设计稿：** [Figma 6696-12844](https://www.figma.com/design/vmbLwcwclGPoT3fWJWv7de?node-id=6696-12844)  
**对照实现：** `src/pages/school/teacher-portrait/teacher-portrait/index.vue` 及子组件

---

## 审查结论概览

| 优先级 | 数量 | 说明 |
|--------|------|------|
| P0 | 1 | 整页右栏网格布局与稿不一致（已在本轮修复） |
| P1 | 0 | 全部 P1 组件级偏离已修复 |
| P2 | 5 | 间距、宽度、图标等细节差异 |
| 通过 | 4 | 左栏与核心卡片组件基本对齐 |

---

## P0：整页右栏布局（已修复）

Figma `6696:12844` 右栏为**多栏网格**，非单列纵向堆叠：

```
┌─────────────────────────────────────────────────────────────┐
│ Row1  [教师画像 760]  gap16  [我的教案 636]                    │
├──────────────────────────────────────────────┬──────────────┤
│ [课堂教学内容评价 1126 全宽]                  │              │
│ [教学风格弹性 642] gap16 [趋势+结构 468 纵向] │ 个人标签云   │
│                                              │ 270 全高     │
├──────────────┬──────────────┬────────────────┴──────────────┤
│ 提问类型 562 │ 语言行为 380 │ 语言可理解度 438               │
└──────────────┴──────────────┴───────────────────────────────┘
```

**修复：** `index.vue` 按上述结构重组 DOM，间距 16px，flex 比例对齐稿面宽度。

---

## P1：组件级偏离

### ~~1. 个人标签云 `6696:13461`~~（已修复 2026-07-03）

| 项 | Figma | 实现 |
|----|-------|------|
| 可视化 | 标签名 + 横向进度条 + 数量 | ✅ `TagCloudModulePanel` 条形列表 |
| 模块 | 固定枚举全量条形列表 | ✅ `tag-sort` 全量枚举 + 四色模块背景 |
| 缺省态 | 6696:20779 四模块全零 | ✅ 未选教师时展示 |

**实现文件：** `components/personal-tag-cloud/`（已移除 ECharts wordCloud）

### ~~2. 提问类型 `6696:13589`~~（已修复 2026-07-03）

| 项 | Figma | 实现 |
|----|-------|------|
| 结构 | 「四何问题」+「布鲁姆分类」双环图 | ✅ 双卡片 + ECharts 饼图 |
| 数据 | 小计「个」 | ✅ counts + subtotal |

**实现文件：** `components/question-type/`（`QuestionTypePanel` + `constants`）

### ~~3. 课堂语言行为 `6696:13645`~~（已修复 2026-07-03）

| 项 | Figma | 实现 |
|----|-------|------|
| 结构 | 左侧环图 + 右侧图例列表 | ✅ 120px 环图 + 180px 图例 |
| 标签 | 表扬鼓励、接纳感受、采纳意见… | ✅ 固定五类 + 份/占比 |
| 小计 | 底部「小计 N 个」 | ✅ |

**实现文件：** `components/classroom-language-behavior/`

### ~~4. 语言可理解度 `6696:13697`~~（已修复 2026-07-03）

| 项 | Figma | 实现 |
|----|-------|------|
| 结构 | 3 个环形 gauge + 综合得分/等级卡片 + 课堂特征 | ✅ `ComprehensibilityGauge` + 卡片区 |
| 维度 | 词汇/句法/内容可理解度 | ✅ 三维度 gauge |
| 等级 | 5 档 pill 标签（6696:21348） | ✅ `grade-mapper` 对齐 |

**实现文件：** `components/language-comprehensibility/`

---

## P2：细节差异

| 模块 | 节点 | 差异 |
|------|------|------|
| 左栏宽度 | 6696:12845 | 稿 260px，实现曾用 320px → 改为 260px |
| 左右间距 | 6696:12844 | 稿 16px，实现曾用 20px → 改为 16px |
| 教师画像 | 6696:12866 | ✅ 主导/辅助间已改为 `MrEditHalfCheckMark` 图标 |
| 教学情境 | 6696:13183 | 稿「课堂氛围低落时」，需求文档「课堂气氛低落时」→ 以需求为准 |
| 页面标题 | — | 稿帧内无「教师画像」大标题，实现已去除 |

---

## 已通过项

| 模块 | 节点 | 结论 |
|------|------|------|
| 我的信息 | 6696:12845 | 头像/姓名/统计行布局与配色一致 |
| 教师列表 | 6696:15974 | 搜索、选中态、分页结构一致 |
| 教师画像卡片 | 6696:12866 | 300px 画像区、30px 姓名、风格 badge、特征标签区一致 |
| 教学风格与弹性 | 6696:13183 | 五风格卡片、330px 雷达、情境标签、中稳定性绿底一致 |

---

## 后续行动建议

1. ~~**Wave Figma Fix-1：** 个人标签云改为条形列表（P1）~~ ✅ 已完成
2. ~~**Wave Figma Fix-2：** 语言可理解度按稿重构图表与字段（P1）~~ ✅ 已完成
3. ~~**Wave Figma Fix-3：** 教师画像主导/辅助间 Miray 图标（P2）~~ ✅ 已完成
4. 各组件缺省态节点逐一对照走查（6696:20xxx 系列）

---

## MCP 拉取记录

| 节点 | 用途 |
|------|------|
| 6696:12844 | 整页 metadata（子层拆分） |
| 6696:12866 | 教师画像有数据态 |
| 6696:12845 | 我的信息 |
| 6696:13183 | 教学风格与弹性特征 |
| 6696:15974 | 教师列表 |
| 6696:13697 | 语言可理解度 |
