# 原始需求：驾驶舱教师画像样式还原

## 背景

组合组件 `teacher-portrait-1`（`mr-teacher-portrait-1`）功能与画布尺寸已交付，用户反馈**多处样式与设计稿差距较大**，需新开一轮 UI 样式还原。

## 关联模块

| 模块 | 状态 |
|------|------|
| `feature/驾驶舱教师画像组合组件` | DELIVERED |
| `fix/教师画像画布尺寸与预览滚动` | DELIVERED |

## 代码位置

- 应用：`apps-development-platform/apps/data-cockpit`
- 目录：`src/views/preview/mr-teacher-portrait-1/`
- identifier：`teacher-portrait-1`

## 设计稿

- Figma（风格 1 / model-1）：https://www.figma.com/design/vmbLwcwclGPoT3fWJWv7de/明睿开发稿-·团队版?node-id=8048-37563&m=dev

## 需求摘要

进行新一轮样式优化：对照 Figma 设计稿，修正当前实现中与设计不一致的视觉细节（字号、色、间距、圆角/边框、装饰、布局比例等）。

## 约束

- 本期仍为 **model-1**；不扩风格 2/3
- 不改业务数据契约与画布投放逻辑（除非样式还原强依赖）
- 先前「文字按容器宽等比缩放」已撤销，保持固定 px（除非本轮方案另行确认）

## 已确认（brainstorming）

| 项 | 结论 |
|----|------|
| 范围 | **整页全量对照**（KPI + 中三栏 + 热力 + 面板壳） |
| 偏差优先级 | 说不清 → **按 Figma Dev Mode 逐项对齐** |
| 对照基准 | **预览页 1920 宽** |
| 主题 | 仅 model-1 |
| 文字缩放 | 保持固定 px（不恢复 `--tp-scale`） |
