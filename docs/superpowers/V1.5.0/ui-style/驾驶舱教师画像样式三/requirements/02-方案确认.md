# 方案确认

**日期：** 2026-08-03  
**模块：** ui-style/驾驶舱教师画像样式三

## 确认结论

用户确认采用 **方案 A**：

- **复用** `board-chart.skin` 既有 model-3 外壳资产（标题图 / 内容底 / 底部 7px 装饰条）
- **不**新导出边框图、不自绘伪元素边框
- 在 `panel-chrome` 补齐 **每面板**底部装饰（对齐 `level-bar-chart` / `mr-active-interaction`）
- 内容区 token 走 `.mr-teacher-portrait--model-3` 紫系变量
- **不影响** model-1 / model-2

## 边框结论摘要

| 部位 | 复用资产 |
|------|----------|
| 标题 | `board/model-3/com-title-bg-small.png`（窄栏）；宽栏可切 `com-title-bg-medium.png` |
| 内容底 | `board/model-3/model-bg.png` |
| 底饰 | `board/model-3/bottom-model-bg.png`（每面板 `end`，高约 7px） |
| 角标 | model-3 **无**左右角标（`showCornerDecoration: false`） |
