# 方案确认

**确认日期：** 2026-08-03  
**方案：** A · ECharts 内容定高 + **相对 1920 稿等比适配**

## 用户确认要点

1. 采用方案 A：高度随内容（行数 × 行高 + 底栏），不均分父级剩余高度。
2. **补充约束：** 1920 设计稿上每格有基准宽高；跨屏时视觉比例保持「像同一张图在缩放」，**不是写死固定 CSS 像素**，而是按容器宽度相对稿面算 `scale`，格子宽高/字号/底栏同比缩放。

## 明确不做

- 方案 B（固定 602 + 内滚）
- 方案 C（DOM 逐行重写）
- 整卡 KPI/中三栏改造（本期只改热力组件；裁切时仅允许最小改 `TEACHER_PORTRAIT_CONTENT_HEIGHT`）
