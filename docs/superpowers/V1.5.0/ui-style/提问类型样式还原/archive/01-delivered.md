# 提问类型样式还原 · 交付快照

**模块：** `ui-style/提问类型样式还原`  
**应用：** data-cockpit 教师画像详情 S6  
**对照 Figma：** `8030:31650`

## 变更摘要

- 双卡改为纵向 gap 20；卡片 pad 14×20、底/边 20%
- 标题行左侧标题 + 右侧「小计 N 个」；去掉徽章与底栏小计条
- 内容行：左饼 80 + 右图例；布鲁姆图例 2+1 分行
- 驾驶舱色：四何天蓝/青/橙/草绿；布鲁姆青/草绿/橙

## 一致性自检

| 检查项 | 结果 | 证据（路径或说明） |
|--------|------|-------------------|
| 空态 vs 有数据 | 通过 | `buildQuestionTypePieOption` 空态等分灰扇区；`isEmpty` 控制动画/tooltip |
| 常量/mock/真数据 | 通过 | `COCKPIT_*_COLORS` 覆盖 viewModel 色；adapter 分数语义未改 |
| 多入口 | N/A | 仅详情页 S6 挂载 |
| 失败/缺省 | 通过 | `buildDefaultQuestionTypeViewModel` 仍走同一 group 组件 |

## 还原度自检

- **节点：** `8030:31650`（提问类型整面板）
- **对照方式：** Figma MCP `get_design_context` + 结构/token 对照
- **偏差清单：**
  - panel-chrome 标题条沿用共享组件，未单独重做斜切标题底
  - 饼图扇区顺序沿用既有 `chartItems`，未强制与稿面扇区角度像素级对齐
- **结论：** 布局与色值已对齐稿面主结构，可报交付；细调饼角度/标题底若需要可再开迭代
