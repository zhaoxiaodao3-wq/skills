# 方案确认（P1）

**日期：** 2026-08-03

| 项 | 结论 |
|----|------|
| 方案 | **A**：组件内统一等比 scale |
| 范围 | **整卡** `mr-teacher-portrait-1`（KPI + 中三栏 + 热力） |
| 影响面 | **仅**教师画像目录及相关特判；不改全局 `ratioX/ratioY`、不改其它 `mr-*` |
| 禁止 | 整卡 `transform: scale`；`vw`/`vh` 作为主适配 |
