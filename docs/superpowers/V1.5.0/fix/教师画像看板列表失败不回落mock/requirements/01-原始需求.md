# 原始需求：教师画像看板列表失败不回落 mock

**日期：** 2026-08-11
**分类：** fix
**实现仓：** `apps-development-platform/apps/data-cockpit`

## 用户原话

> 教师列表请求失败目前不要回退到 mock 数据了，请求失败无非就是拿不到教师的列表还有科目列表的数据，真实接口请求失败或者请求数据为空肯定都是按接口来的，为空就展示空状态的样子就行。

## 目标

- `/teachers` 请求失败或返回空数据时，教师列表与科目列表展示空状态，不再回落 mock。
