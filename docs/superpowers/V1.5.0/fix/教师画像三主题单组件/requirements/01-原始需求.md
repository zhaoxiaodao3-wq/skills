# 原始需求：教师画像三主题单组件

**日期：** 2026-08-03  
**分类：** fix  
**关联实现：** `apps/data-cockpit/src/views/preview/mr-teacher-portrait*`  
**参考：** `mr-active-interaction`（单目录 + `theme` prop + `--model-1/2/3`）

## 用户原话

> apps\data-cockpit\src\views\preview\mr-active-interaction 能参考一下这个组件帮我修改当前组件吗，就是不需要创建三个组件去区分三个样式

## 目标

教师画像与积极互动一致：一套组件覆盖 model-1/2/3，靠 `theme` / identifier 后缀切换皮肤，禁止再建 `mr-teacher-portrait-2`、`mr-teacher-portrait-3`。
