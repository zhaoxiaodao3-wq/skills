# 方案确认

**日期：** 2026-08-03

| 项 | 结论 |
|----|------|
| 方案 | 对齐 `mr-active-interaction`：单目录 `mr-teacher-portrait` + `theme` + BEM `--model-*` |
| 改动 | 重命名目录/入口；`isTeacherPortraitCmpnt` 认 `teacher-portrait` 及 `-1/-2/-3`；panel-chrome 补 model-2/3 内容底 |
| 不做 | 另开三个组件目录；本期不做 model-2/3 全新 Figma 细调（沿用 board 皮肤 token，现有 SCSS 已有色 token） |
