# 原始需求

## 页面

H5 教师画像分享报告：`E:\code\H5\src\pages\share\teacherProfile`

## 问题

图表 hover 提示因内容过长被截断。

## 范围（用户收窄）

**仅修「提问类型」组件中的「布鲁姆分类」饼图** tooltip，不改其它图表、不改公共 `CHART_TOOLTIP_BASE` / `MrEcharts`。

## 关联代码

- `components/QuestionTypeSection.vue` — 含四何（sihe）+ 布鲁姆（bloom）两块
- `components/QuestionTypePanel.vue` — 共用面板
- `chart-options/question-type-chart.ts` — `buildQuestionTypePieOption`
- 根因同前：`CHART_TOOLTIP_BASE.confine: true` + `MrEcharts overflow:hidden`；布鲁姆分类名较长更易截断
