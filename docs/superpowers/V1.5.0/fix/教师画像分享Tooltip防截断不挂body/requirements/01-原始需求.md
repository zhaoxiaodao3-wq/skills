# 原始需求

## 背景

上一轮去掉 `appendTo: 'body'` 后，定位正常，但 **hover 内容过多时又被截断**（`confine: true` + `MrEcharts overflow:hidden` + 小图槽）。

## 目标

同时满足：

1. 长文 tooltip **不被截断**  
2. **不挂 body**（避免跑页底、撑高页面）  
3. 仍用 ECharts 原生 tooltip  

## 约束

禁止再使用 `appendTo: 'body'`。
