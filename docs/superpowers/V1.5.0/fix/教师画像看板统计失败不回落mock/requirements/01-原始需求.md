# 原始需求：教师画像看板统计失败不回落 mock

**日期：** 2026-08-11
**分类：** fix
**实现仓：** `apps-development-platform/apps/data-cockpit`

## 用户原话

> 同页面的另一个接口 `/classroomCmpnt/teacherStyleDashboard/statistics` 也帮我先换成 `66666` 这个 id 请求数据对接，并且同理，目前要切换到真实接口，请求失败或者空数据不允许回退到 mock 数据，只能展示为空状态。

## 目标

- `statistics` 使用真实 `dataSourceBindId`（页面 URL query）请求。
- KPI / 风格分布 / 标签 / 热力请求失败或空数据时展示空状态，不再回退 mock。
