# 原始需求：教师画像看板 teachers 参数补全

**日期：** 2026-08-11
**分类：** fix
**实现仓：** `apps-development-platform/apps/data-cockpit`

## 用户原话

> 这个教师画像组件，`/classroomCmpnt/teacherStyleDashboard/teachers` 这个接口参数要齐全：姓名为空就传空字符串；性别默认选全部，也是空字符串；风格类型没选就传空数组；科目默认选全部就传空字符串。

## 目标

- `/teachers` 请求体始终包含 `userName / mainSubjectName / genderStr / styleTypeNames` 四个字段，空值用空字符串或空数组，不再省略。
