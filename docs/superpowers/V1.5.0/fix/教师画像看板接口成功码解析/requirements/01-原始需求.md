# 原始需求：教师画像看板接口成功码解析

**日期：** 2026-08-11
**分类：** fix
**实现仓：** `apps-development-platform/apps/data-cockpit`

## 用户原话

> 接口已经请求成功了，科目列表的内容跟教师列表都不是接口的数据。

## 根因

- `/classroomCmpnt/teacherStyleDashboard/teachers` 响应 `code: 200`（实测 `code=200 msg=ok`，`data.subjectOptions` / `data.teachers` 齐全）。
- `use-teacher-style-dashboard.ts` 的 `unwrapPayload` 只认 `code 0`，把 `200` 判为失败，真实数据未写入，页面回退 mock。

## 目标

- 成功码与详情页一致：`0 / 200 / '00000'`（无 code 视为成功）。
