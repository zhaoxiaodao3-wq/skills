# 原始需求：教师画像头像一倍四倍图

**日期：** 2026-08-06
**分类：** fix
**实现仓：** `apps-development-platform/apps/data-cockpit`
**预览页：** `/data-cockpit-app/preview/teacher-portrait-detail`

## 用户原话

> 该页面教师基本信息的头像需要替换，新的头像路径在 `C:\Users\YIL\Documents\WXWork\1688857489743891\Cache\File\2026-08\导出\导出`，有一倍图和四倍图，首先你按照之前头像的命名规则先帮我把下面的图片所有名字给修改过来，之后的逻辑就是默认加载一倍图，然后动态加载四倍图，如果四倍图加载成功就替换一倍图，一倍图和四倍图之后都会存放在 oss 上。

## 现状

- 教师风格头像 OSS URL 由 `utils/portrait-url.ts` 按 `slug1__slug2__gender.png` 生成（如 `warm-guiding__rational-inspiring__male.png`）。
- 教师基本信息组件 `detail/components/teacher-basic-info/teacher-basic-info.vue` 直接渲染 `data.portraitUrl`（1x）。
- 导出目录 `导出/1x/`、`导出/4x/` 提供 5 种教学风格两两组合 × 男女共 20 组头像，4x 文件带 `@4x` 后缀。
- 空头像原引用为 OSS `teacher-profile/.../teacher-portrait-empty.png`；用户提供本地 `C:\Users\YIL\Downloads\img-empty.png` 作为新空头像。

## 已完成

- 按旧命名规则批量重命名导出目录 40 张图片：
  - `1x/激情讲授型 × 权威传授型-男.png` → `passionate-lecturing__authoritative-imparting__male.png`
  - `4x/激情讲授型 × 权威传授型-男@4x.png` → `passionate-lecturing__authoritative-imparting__male@4x.png`

## 目标

- 头像默认加载 1x 图；随后动态预加载 4x 图，加载成功则替换显示，失败保持 1x。
- 1x / 4x 图片后续均存放于 OSS，代码按现有命名规则构造 OSS URL。
- 空头像使用 `img-empty.png`，替换原空头像引用。

## 约束

- 不改接口契约与 `PersonalFeatureSlice` 类型（除非确认需要）。
- 只替换教师基本信息头像；教师列表/教师卡片头像不在此范围。
