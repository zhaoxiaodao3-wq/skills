# 原始需求：教师画像看板 dataSourceBindId 真实获取

**日期：** 2026-08-11
**分类：** fix
**实现仓：** `apps-development-platform/apps/data-cockpit`

## 用户原话

> dataSourceBindId 替换为项目里面真实的获取方式，删除当前 mock 写死的方式。

## 目标

- 看板 `/statistics`、`/teachers` 请求的 `dataSourceBindId` 改从页面 URL query 获取，删除 `'66666'` 硬编码。
