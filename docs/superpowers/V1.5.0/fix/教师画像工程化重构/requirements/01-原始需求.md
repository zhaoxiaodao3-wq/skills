# 原始需求

对 `mr-teacher-portrait-1` 做工程化重构（不改视觉）：

1. 领域类型从 mock 抽到 `types/`
2. 组件经 `adapters/` 取数（当前仍走 mock，便于换真接口）
3. 热力 / 风格分布 layout 外提
4. 「数据态」预览开关仅 `import.meta.env.DEV` 显示

**工作目录：** `apps-development-platform/apps/data-cockpit/.../mr-teacher-portrait-1`
