# 原始需求

## 页面

H5 教师画像分享报告：`E:\code\H5\src\pages\share\teacherProfile`

## 问题

hover 卡片定位不准（出屏、跑到页底、撑高页面）。确认均为 ECharts 原生 tooltip；异常来自 `appendTo: 'body'` + `confine: false`。

## 已确认方案

**方案 A：** 去掉 body 挂载，恢复 `confine: true`；截断靠局部 `overflow: visible` / 换行；**并全量检查所有图表**是否存在同样配置。

## 全量排查结果（只读）

| 图表 option 文件 | tooltip 策略 | 风险 |
|------------------|--------------|------|
| `chart-animation.ts` `CHART_TOOLTIP_BASE` | `confine: true`，无 appendTo | 正常基线 |
| speaking-behavior / clarity / teaching-style-trend / teaching-style-radar / my-lesson-plan / content-eval **donut** | 继承 BASE | **无** body 挂载问题 |
| score-trend | 自建，`confine: true` | **无** |
| **question-type**（布鲁姆 `escapeContainer`） | `confine: false` + `appendTo: 'body'` | **有 —— 须改** |
| **classroom-content-eval 雷达** | `confine: false` + `appendTo: 'body'` | **有 —— 须改** |

分享目录下仅上述 **2 处** 存在同类问题；其余图表无 `appendTo` / `confine: false`。
