# 补充收敛（2026-07-31）

## 用户确认

| 项 | 结论 |
|----|------|
| 方案 | **A：双场景** |
| 设计宽 | 页面宽 **1920**（内容区左右边距同稿，约 30 → 内容宽约 1860） |
| 编辑拖入 | **自动铺满一页**（忽略落点漂移；left/top 按边距吸附） |
| 预览 | 按内容真实高度占位，**整页可纵向滚动**，禁止用视口 ry 把整卡压进一屏 |
| 顶偏移（2026-07-31 补） | 组件从 KPI 汇总起、**不含顶栏标题**；编辑 `top` = **146**（非 30）；一页高改为 **904** |

## 关联

- identifier：`teacher-portrait-1`
- 父需求：feature/驾驶舱教师画像组合组件
- 内容高度参考：范围 B ≈ KPI+中三栏+底热力（约 1454；以 Figma/现实现为准换算）
