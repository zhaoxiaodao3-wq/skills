# 原始需求

**类型：** feature  
**模块：** 驾驶舱教师画像详情空状态  
**页面：** data-cockpit 教师画像详情预览页  
**预览：** `http://localhost:8100/data-cockpit-app/preview/teacher-portrait-detail?...`

## 需求摘要

1. 检查详情页**全部组件**是否已覆盖空数据状态；未做的补齐。
2. 空状态对齐校端 `teacher-portrait`：**各组件内建空数据态**（图表骨架 / `--` / `暂无`），**不用**整卡插画占位。
3. 增加 DEV **开关**：可在「有数据」与「空状态」之间切换，便于联调与验收。

## 实现落点（预期）

- `apps/data-cockpit/.../mr-teacher-portrait/detail/`（详情页与各 panel）
- `use-detail-profile` empty builders + `forceEmptyPreview`
- 文档模块：`docs/superpowers/V1.5.0/feature/驾驶舱教师画像详情空状态/`
