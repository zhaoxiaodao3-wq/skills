# AI课堂配置 · 录播重复诊断开关 · 原始需求

**日期：** 2026-07-30  
**分类：** feature  
**版本：** V1.5.0（文档实体：frontend-local）  
**提出方式：** `/harness`

## 需求原文

页面（测试环境）：  
https://test.mirayai.com:31594/app/basic-data/manager/school/list/config/aiLoginConfig?id=2054408492407742465  

在「AI课堂单点登录配置」tab：

1. 将 tab 名称改为「AI课堂配置」
2. 选中该 tab 后，右侧内容区新增一块（风格对齐现有「AI课堂【学校】平台单点登录链接地址」区域）：
   - 标题：AI课堂录播视频
   - 下方 Element Plus Switch：标题「支持重复诊断分析」，默认关闭
   - 切换开关后须点「保存」调保存接口，成功才算生效；失败则 Switch 恢复为进入 tab 时接口查到的初始状态
   - 保存按钮防抖（项目已有 `debounceAsync` from `zf-utilz`、`useDebounceFn` from `@vueuse/core`）
   - 保存按钮 loading，成功/失败后结束 loading
   - 暂无真接口，先 mock，后续对接
3. 不允许影响原有功能；新功能逻辑闭环

## 仓库定位结论（已确认）

- **实现仓库：** `E:\code\two\frontend`（mr-platform/frontend）
- **开发分支：** `dev/lianglinbin`（基于 `origin/develop`）
- **学校配置页：** `src/pages/app/basic-data/manager/school/list/config.vue`（tab 文案）
- **内容页：** `src/pages/app/basic-data/manager/school/list/config/aiLoginConfig.vue`
- **防抖：** 项目已有 `debounceAsync`（`zf-utilz`），页面中广泛使用；另有 `@vueuse/core` 的 `useDebounceFn`（自动导入）
- 本需求范围：**学校**侧 `aiLoginConfig`（用户所给 URL）；教育局侧同类页暂不改，除非另行确认

**说明：** 文档仍写在课堂前端仓的 Superpowers `V1.5.0`；**代码改动只在 two/frontend。**
