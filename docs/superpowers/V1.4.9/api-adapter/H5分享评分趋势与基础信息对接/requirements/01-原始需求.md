# H5 分享评分趋势与基础信息对接

## 项目

`E:\code\H5` — 教师画像分享页 `/teacher-profile`

## 需求

接口已补充字段，前端需对接真实数据，替换 Mock：

1. **评分趋势**：`reportContent.scoreTrendList`
2. **头图基础信息**：`teacherBasicInfo`（姓名、性别、主教学科、总时长）

## 现状

- 评分趋势：`adaptClassroomContentEval` 固定 `createMockScoreTrend()`
- 头图：只读 `data.basicInfo`，空则用 `MOCK_SHARE_BASIC_INFO`

## 期望

- 有 `scoreTrendList` 时渲染真实趋势；空数组为空态（不再灌 Mock）
- 头图优先读 `teacherBasicInfo`；有值则展示真实姓名/性别/学科/时长
- 字段映射对齐 PC `scoreTrend` adapter（`name`/`scoreLevel`/`score`/`reportType`/`genTime`）

## 关联路径

- `adapters/adapt-share-get-report.ts`
- `adapters/adapt-classroom-content-eval.ts`
- `types/share-report.ts`
