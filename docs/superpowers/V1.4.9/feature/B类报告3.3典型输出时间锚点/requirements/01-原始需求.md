# B 类报告 3.3 典型学生输出摘录 · 时间锚点补充

**版本：** V1.4.9

## 原始需求

B 类报告 3.3「典型学生输出摘录」补充时间锚点逻辑（若尚未接入）。

## 背景

- [02-B类时间锚点.md](../AB类报告点击时间播放视频/requirements/02-B类时间锚点.md) 白名单第 5 项：3.3「典型学生输出摘录」列表项内时间可点
- 当前 `CalcProcessDisclosureRow` 通过标题含「典型学生输出摘录」开启 `enableTimeAnchor`
- B mapper 3.3 已用 `calcProcessRow` + 该标题渲染
- 需确认并加固：显式 item 级 `timeAnchor`、B 类 `formatTypicalOutputLine` 对 `时:分:秒` 时间戳识别

## 关联路径

- `CalcProcessDisclosureRow.vue`
- `classroom-content-analysis-report.ts`（`CalcProcessDisclosureItem`）
- `classroom-content-analysis-b.mapper.ts`（3.3 calcProcessRow）
- `report-display-format.ts`（`formatTypicalOutputLine` B 变体）
- `type-b-chapters.ts` mock
