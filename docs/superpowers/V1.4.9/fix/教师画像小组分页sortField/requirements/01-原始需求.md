# 教师画像小组分页 sortField

**分类：** fix  
**日期：** 2026-07-21

## 原始诉求

教师画像页面调用 `/api/quota/group/page`（`getQuotaGroupPage`）的地方，多加一个入参：

```json
"sortField": "memberCount"
```

## 定位

- 调用链：`TeachingGroupContainer` → `fetchTeachingGroupPageData` → `ctx.getQuotaGroupPage` → `service.quotaNew.getQuotaGroupPage` → `POST /quota/group/page`
- 改动点：`src/pages/school/teacher-portrait/components/teaching-group/teaching-group-api.ts` 内请求参数

## 范围

仅教师画像教学小组分页；不改其它业务对 `getQuotaGroupPage` 的调用。
