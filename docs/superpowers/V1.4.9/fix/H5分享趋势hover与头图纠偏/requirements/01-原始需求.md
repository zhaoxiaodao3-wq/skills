# H5 分享趋势 hover 与头图纠偏

## 背景

上一模块「H5分享评分趋势与基础信息对接」已 DELIVERED，但联调发现未完成：

1. **评分趋势 hover 卡片**与 PC 不一致（H5 现为默认 axis tooltip，缺 PC 的评分/评级/课例名称/生成时间卡片）
2. **头图**姓名、性别、主教学科、上课总时长、头像不对；文案「主要科目」应改为「主教学科」

## 代码侧已定位差异

- Hover：PC `score-trend-chart-options.ts` 使用 `trigger: 'item'` + `buildTooltipHtml`；H5 `score-trend-chart.ts` 无自定义 formatter、未按点绑定 report 元数据
- 文案：`TeacherPortraitHero.vue` 现为「主要科目：」
- 头图数据：仍可能因 Mock 字段级回填、字段路径/别名不全、性别码未归一导致展示与立绘错误

## 期望

- Hover 内容与交互对齐 PC（评分、评级含色点、课例名称、生成时间格式）
- 头图正确展示接口 `teacherBasicInfo`；文案为「主教学科」
- 有真实基础信息时勿用 Mock 姓名/性别等「掺假」

## 关联路径（H5）

- `chart-options/score-trend-chart.ts`
- `components/TeacherPortraitHero.vue`
- `adapters/adapt-share-get-report.ts`
- `adapters/adapt-classroom-content-eval.ts`（排序等，如需）
