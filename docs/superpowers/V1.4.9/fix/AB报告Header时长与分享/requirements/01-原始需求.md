# AB 类报告 Header · 课堂时长与分享按钮调整

## 一、页面

课堂教学内容分析报告（A 类 / B 类）顶部 `ReportHeroHeader`。

## 二、调整内容

1. **课堂时长**
   - 去掉时钟旁的时长图标（`icon-schedule`）
   - 将「课堂时长：xxx」挪到第二行（科目 / 年级 / …）**末尾**

2. **分享按钮**
   - 在原课堂时长位置（标题行右侧）放入二维码分享按钮
   - 使用已交付的全局组件 `AppShareLink`，Header 深色底用 `variant="ghost"`

## 三、范围

- 改动：`ReportHeroHeader.vue`（A/B 共用，一次改两处生效）
- 复用：`@/components/AppShareLink`
- 不改评分卡片、摘要等其它 Header 区块
