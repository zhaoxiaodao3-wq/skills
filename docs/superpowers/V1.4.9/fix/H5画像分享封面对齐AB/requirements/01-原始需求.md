# H5 画像分享封面对齐 A/B

**来源：** `/harness`（同域 JPG 方案无效；用户要求对齐 A/B）  
**日期：** 2026-07-21  
**目标仓库：** `E:\code\H5`

## 问题

教师画像分享无自定义封面。

## 硬约束（用户确认）

- 封面 **禁止更换**，必须使用已给定 OSS：  
  `https://mirayai-iot-dev.oss-cn-shenzhen.aliyuncs.com/image/h5/share/teacher-profile.png`  
- **禁止**临时改用 A/B 封面、禁止同域 `/share/*.jpg` 替代

## A/B 做法

`imgUrl` 写死 OSS HTTPS 常量 + `initWxConfig` + `setupWxShare`。

## 方案 A（已确认）

画像对齐 A/B 配置方式；封面常量恢复为上述 URL，删除 `resolveTeacherProfileShareCover` / 同域路径逻辑。
