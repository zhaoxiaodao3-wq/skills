# H5 教师画像页面标题

**分类：** fix（补充需求）  
**日期：** 2026-07-21  
**目标仓库：** `E:\code\H5`  
**关联：** [H5新增类型分享页](../../feature/H5新增类型分享页/requirements/01-原始需求.md)（已 DELIVERED）

## 原始诉求

从教师画像跳转到 H5 `/teacher-profile` 时，**网站标题**（浏览器 / 微信顶栏 `document.title`）应显示为「教师画像」。

## 现状

- 路由已配置 `meta.title: '教师画像'`、`noAuth: true`
- `main.ts` 在 `noAuth` 分支直接 `next()`，**未执行** `document.title = to.meta.title`
- 因此仍显示默认站点标题（如「智慧校园」或入口页标题）
