# 报告预览与下载 · Tab 顺序调整

## 一、需求

AI 教学诊断详情页侧栏「报告预览与下载」下子 Tab 顺序调整为（展示名含 mergeLabel「AI」前缀）：

1. **AI教学分析报告**
2. **AI课堂实录报告**
3. **AI课堂分析报告**

## 二、现状

`buildReportPreviewMenu` 当前顺序为：课堂实录 → 课堂分析 →（A/B 时追加）教学分析。

## 三、改动范围

- 文件：`src/pages/analysis-web/ai-teaching-diagnosis.vue`
- 函数：`buildReportPreviewMenu`（及必要时加载占位菜单，若与正式顺序不一致再对齐）
- A/B：三项按上述顺序；G 类：仅有实录 + 分析时顺序为实录 → 分析（无教学分析项，行为保持）

## 四、非目标

- 不改路由 path、不改 tab 文案（仍用现有 label + mergeLabel）
- 不改其它侧栏分组顺序
