# 分享链接 loading 与超时 · 原始需求

**来源：** 用户 `/harness`  
**日期：** 2026-07-21  
**分类：** fix

## 诉求

确认并补齐分享链接按钮的：

1. 防连点（请求锁，不加时间防抖）
2. 请求超时
3. 明确的 loading 视觉反馈

## 已确认方案 A

- 按钮加明确 loading（转圈）
- `createShare` 显式 `timeout`（15s），超时走「分享失败」
- 保留现有 `requesting` 锁定，**不加**时间防抖
