# 课堂语言行为饼图单位 · 原始需求

**来源：** `/harness`  
**日期：** 2026-07-28  
**分类：** fix

## 诉求

教师画像「课堂语言行为」模块中，**hover 饼图（环形图）时 tooltip 的单位**由「份」改为「个」。

## 现状

- PC：`classroom-language-behavior/chart-options.ts` 使用 `formatPieTooltipWithUnit(params, '份')`
- 同页「提问类型」饼图已用「个」，语言行为不一致

## 范围边界（待 P1 确认）

- 仅改 hover tooltip 单位，还是图例文案一并改
- 是否同步改 H5 分享页同模块
