# H5教师画像个人标签云 · 原始需求

**版本：** V1.4.9  
**分类：** ui-style  
**日期：** 2026-07-22  
**目标仓库：** `E:\code\H5`  
**关联：** 语言可理解度已 DELIVERED；Figma 顺序本模块为收尾模块

## 目标

在教师画像分享页、语言可理解度下方挂载「个人标签云」（H5 稿为四类横向进度条列表，非词云气泡）。

| 项 | 内容 |
|----|------|
| UI | Figma `7485:15318` |
| 数据 | `reportContent.personalTagCloud`（已在 getReport） |
| PC 参考 | `personal-tag-cloud/`（只读） |

## 数据结构（样例）

- `totalReportCount`
- `tagCategories[]`：`categoryName` / `categoryType` / `tags[]`（`tagValue` / `count` / `rank`）
- 四类：话语特色、情感风格、权力关系、学科适配（学科名可拼在标题）

## 不做

改 PC；改 getReport / 分享壳；其它已交付模块。
