## Harness 门禁

开发类任务（新功能、改页面、修 bug、实现需求）：

1. 用户入口：`superpowers-harness-run` 技能或 `/harness <需求>`
2. 读取 `docs/superpowers/HARNESS_RULES.md` 与 `SUPERPOWERS_RULES.md`
3. 改 `src/` 前运行 `pnpm harness:status` 与 `pnpm harness:check`
4. 目录规范遵循 `superpowers-demand-workflow`；未完成 spec/plan 不得改 `src/`
5. 禁止写入 `docs/superpowers/specs/` 等旧扁平路径
