## Superpowers + Harness + Skill 路由（开发流程）

开发类任务（新功能、改页面、修 bug、实现需求）：

1. **用户入口**：`superpowers-harness-run` 技能或 `/harness <需求>`；入口后先分档（轻量 / 标准 / 全量）
2. 读取 `docs/superpowers/HARNESS_RULES.md`（尤其 **§3.1 暂停点边界**）与 `SUPERPOWERS_RULES.md`
3. 流程：`P1 → create-demand → spec(P2) → plan → skill routing → P3 → READY_TO_DEV → 开发 → 交付归档`
4. **先文档后实现**：禁止先改 `src/` / 模板等再补 requirements/spec/plan
5. **「确认 / 只改 HTML」≠ 可开发**；P2 须 `spec OK`，P3 须 `Inline`/`SDD`（见 HARNESS_RULES §3.1）
6. **writing-plans 后**：`node .agents/routing/router.mjs --annotate <plan>`，写入 plan 各 Task
7. 改实现前：`pnpm harness:status` + `pnpm harness:check`（须 `READY_TO_DEV`）
8. 开发遵循 plan 内 skill 标注；目录规范遵循 `superpowers-demand-workflow`
9. 禁止写入 `docs/superpowers/specs/` 等旧扁平路径
10. **官方 skill**：升插件更新；Bounded 短设计不能绕过本仓短 spec/plan
