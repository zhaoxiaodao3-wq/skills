# Agent 项目地图

## 技术栈

- {{FRAMEWORK}}
- {{UI_LIB}}
- {{STATE}}

## 目录导航

| 路径 | 用途 |
|------|------|
| src/pages/ | 页面组件 |
| src/components/ | 公共组件 |
| docs/superpowers/current/ | 当前版本需求文档 |
| docs/superpowers/HARNESS_RULES.md | Harness 门禁 + §3.1 暂停点边界 + Skill 路由 |
| scripts/harness/ | Harness 校验器 |
| .agents/routing/SKILL_ROUTING.md | Skill 路由图（权威来源） |
| .agents/routing/router.mjs | Skill 路由 CLI（`--annotate` / Mode B） |

## 开发流程（强制）

1. 读取 `HARNESS_RULES.md`（尤其 §3.1）与 `SUPERPOWERS_RULES.md`
2. 入口：`superpowers-harness-run` 或 `/harness`；先分档
3. 标准/全量：`P1 → spec(P2) → plan → annotate → P3 → READY_TO_DEV → 开发`
4. **先文档后实现**；「确认 / 只改 HTML」≠ 可开发
5. 官方 Bounded 短设计不能绕过本仓短 spec/plan

## 禁止

- 先改实现再补文档
- 跳过 P2/P3 改实现
- 写入旧扁平 `docs/superpowers/specs/` 等路径
