# 项目 AI 上下文

## Git 提交信息（强制中文）

为本仓库生成 Git commit message 时（含 Cursor Source Control 的 ✨ 自动生成），必须遵守：

1. 格式：`<type>: <中文描述>`（type 用 feat/fix/refactor/chore 等英文前缀）
2. 冒号后必须是中文，说明改动目的，不要用英文描述
3. 示例：`feat: 新增核心教案只读预览页`、`fix: 修复课堂信息章节字段未显示`

## Superpowers + Harness + Skill 路由（开发流程）

开发类任务（新功能、改页面、修 bug、实现需求）：

1. **用户入口**：`superpowers-harness-run` 技能或 `/harness <需求>`
2. 读取 `docs/superpowers/HARNESS_RULES.md` 与 `SUPERPOWERS_RULES.md`
3. 流程：`create-demand → brainstorming → writing-plans → skill routing 标注 plan → 开发 → 交付归档`
4. **writing-plans 后**：读 `.agents/routing/SKILL_ROUTING.md`，运行 `node .agents/routing/router.mjs --annotate <plan>`，将建议 skill 写入 plan 各 Task
5. 改 `src/` 前运行 `pnpm harness:status` 与 `pnpm harness:check`（须 `READY_TO_DEV`）
6. 开发时遵循 plan 内 skill 标注；目录规范遵循 `superpowers-demand-workflow`
7. 禁止写入 `docs/superpowers/specs/` 等旧扁平路径
